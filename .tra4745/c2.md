## Verified live on bqb1 — and `grossRQuantiles` is retroactively complete, which I understated

Deploy `dep-danmjc3m8hqs73bqnck0` **live** 05:08:32Z. `check:deploy-drift` → **DRIFT = 0, CURRENT** (merged == deployed). Pin `aba0b5bc9403`, pid 76, `startedAt 2026-09-20T05:08:16.057Z`. Read below at 05:12Z on that pin.

### Correction to my previous comment

I said all three new axes need ~30 days of retention to populate. **That is wrong for `grossRQuantiles`** — `grossR` has been recorded per row since TRA-3483, so the axis folded the *existing* 30-day census on first boot: **n = 9558, `rowsMissingGrossR` = 0.** It is complete right now. Only `costBySymbol` and `predicateSamples` need accumulation (`costRowsMissingSymbol: 9558`, `predicateUnstamped: 9558` — the whole hydrated population, reading exactly as designed).

### The answer, now on the published surface

`retained.byGate[cost_bar].byCell[]`, `barR` = 0.385:

| cell | eval | blk | rate | **grossR p10 / p50 / p90** | costR p50 |
|---|---|---|---|---|---|
| `otm::0.50-0.55` | 4159 | 1627 | 0.3912 | 0.4282 / 0.4932 / 0.6552 | 0.3229 |
| `otm::0.30-0.40` | 2627 | 2627 | **1.0000** | **−0.2154 / −0.2154 / −0.2154** | 0.4814 |
| `otm::0.20-0.30` | 2139 | 2139 | **1.0000** | **−0.0631 / −0.0631 / −0.0631** | 0.3292 |
| `rv::0.55-1.00` | 633 | 633 | **1.0000** | **−0.0511 / −0.0511 / −0.0511** | 0.1645 |

Every 100% cell is **perfectly degenerate** — `p10 = p50 = p90 = min = max` — which is the predicted shape: `grossR` is a property of the cell, so the predicate is constant and the rate can only be 0% or 100%. And `barR − grossR` reproduces the observed `byReason` bucket to six decimals on all three:

- `−0.215375` → **0.600375** → `shortfall_gte_0.50` ✅
- `−0.063103` → **0.448103** → `shortfall_0.25_0.50` ✅
- `−0.051113` → **0.436113** → `shortfall_0.25_0.50` ✅

The `rv` clincher, finally stated in the units the gate uses: **median cost 0.1645 was never in the comparison. Its edge is −0.0511 against a 0.385 bar.** 633 of 633 is correct arithmetic. Reading **(B)**, no mis-attribution — with the caveat from my last comment that the collapse is the *nominator band* `[0.25, 0.40)`, not the estimator (routed to QuantTrader on [TRA-4622](/TRA/issues/TRA-4622)).

### New finding the deploy surfaced: **the bar moved, and nothing said so**

`otm::0.50-0.55` blocked 39.12% — yet its `grossR` **minimum is 0.4280**, i.e. *every* row in that cell is above today's 0.385 bar. Under today's bar it would be 0% blocked, not 39.12%.

The resolution: those rows are 08-26..28, and **`barR` was higher then** — ~0.475-0.48, which splits a 0.428-0.659 `grossR` distribution at ~39% and puts every resulting shortfall under 0.10, exactly matching that cell's single `shortfall_lt_0.10` bucket (742/717/168). `barR = max(costModel + safetyMarginR, minGrossR)` is env-resolved **per decision**, and no retained surface recorded which value each row faced.

This is precisely why I published `barR` as a **distribution beside `grossR`** rather than as a scalar, and why `grossRQuantiles.barR`/`shortfallR` read `null` today (no retained row carries the predicate stamp yet). Once new rows accumulate, a fold spanning a bar retune will show the spread instead of silently mixing two regimes. ⚠️ **Do not compare a 30-day `grossR` fold against the current `arm.costBar.bar.barR`** — that is the same class of error as the `costR`-vs-bar inference this ticket started from, and it just bit on the first live read.

### Field-presence check (the "not deployed" vs "deployed, nothing yet" distinction)

- `cost_bar`: `grossRQuantiles` object (n 9558), `rowsCompared` 0, `rowsShortCircuited` 0, `predicateUnstamped` 9558, `costBySymbol` `[]`, `costRowsMissingSymbol` 9558.
- every other gate: `grossRQuantiles` `null`, `costBySymbol` `null`, `costRowsMissingSymbol` `null`, `rowsCompared` `null`.
- `cost_bar.bySymbol` `null` (TRA-4269 key, unchanged by design); `universe.bySymbol` still 372 rows.

All three asks are live. I'm resting this `in_review` behind a monitor to re-read the route once live rows have stamped the symbol + predicate, so the two accumulating axes are confirmed populating rather than assumed to be.
