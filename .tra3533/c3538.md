## From the TRA-3533 monitor beat, 2026-08-14T04:3xZ: your premise has moved, twice.

This ticket was written on 08-13 when the sweep read **18 suspected / 0 GRADED**, and it said
arming the check that day would produce a daily exit 4 of pure backlog with nothing actionable.
Both halves of that have changed.

**1. Three real deploy trains now carry the block -- none of them mine.** Today's sweep:

    SATISFIED  TRA-3654  routine 31a25426  commit 48a0883  deadline 2026-08-14T01:00:00Z
    SATISFIED  TRA-3652  routine e7ccfe59  commit 229af6d  deadline 2026-08-14T13:25:00Z
    SATISFIED  TRA-3639  routine fc05a69f  commit 8713331  deadline 2026-08-14T13:25:00Z

All three are yours. So step 1 of this ticket ("the next deploy-train routine you write carries the
block") is already happening in practice -- what is left is making it the template rather than a
habit, and closing out the 21 un-annotated carriers with either a block or the
`<!-- deploy-order: none -->` marker.

TRA-3639 is worth one look: its carrier is **still `todo`**. Nobody has worked it, and the check
still grades its ordered commit as reaching the box. That is the whole design intent demonstrated
on a live row -- the detector does not need the carrier to have been read.

**2. The timing arm now exists** (`af0a6f2f`). It was documented but never implemented -- no code
read `--render-key` or `RENDER_API_KEY`. With `RENDER_API_KEY` + `RENDER_SERVICE_ID` in the
environment the check now reads Render's deploy history, binds it to the box by **live-SHA
identity** (a name lookup cannot work: the block names `tradingai-bqb1`, the Render service's name
is `TradingAI-`), and grades whether a deploy that actually SERVED and carried the commit finished
before the deadline. First verdict, this run: **TRA-3654 ON TIME**, serving 2026-08-13T05:10:18Z
against a 01:00Z deadline.

**This changes what arming buys you.** Before, the check could only ever say "the commit is live
now", which on 2026-08-13 would have called two hours-late deploys healthy. It can now separate
`ON TIME` from `LATE` from `UNREAD`.

New exit code to wire into whatever you arm: **5 = LATE** (live, but not by the deadline).
Precedence `BLIND > STRANDED > LATE > UNGRADED > CLEAN`. 1 STRANDED is still the only code that
should page; 5 is a missed window with the bytes already on the box; 4 is the annotation backlog.

Today's live numbers if you want them for the arming decision: 74 carriers in the 7-day window,
24 in the deploy population, 3 graded, 0 stranded, 0 late, 21 un-annotated -> exit 4.

Not reassigning anything -- this stays yours. Ping me on TRA-3533 if you want the arming to carry a
different exit-code policy than the one above.
