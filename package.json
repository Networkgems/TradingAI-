{
  "name": "trading-app",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter desktop tauri dev",
    "build": "pnpm --filter desktop tauri build",
    "render-build": "pnpm lint || echo '[render-build] lint reported issues (non-blocking for deploy; fix via CI/pre-commit)'; pnpm --filter @trading-app/shared build && pnpm --filter @trading-app/engine build && pnpm --filter @trading-app/backtest build && pnpm --filter @trading-app/server build && pnpm --filter desktop vite:build",
    "server:dev": "pnpm --filter @trading-app/server dev",
    "server:build": "pnpm --filter @trading-app/server build",
    "server:start": "pnpm --filter @trading-app/server start",
    "web:dev": "concurrently \"pnpm server:dev\" \"pnpm --filter desktop vite:dev\"",
    "web:build": "pnpm -r --filter './packages/**' build && pnpm --filter desktop vite:build",
    "render:status": "node scripts/render-deploy-status.mjs",
    "typecheck": "pnpm -r --filter \"@trading-app/*\" build && pnpm -r typecheck",
    "lint": "eslint . --max-warnings 0",
    "check:stale-js": "node scripts/check-stale-js.mjs",
    "check:cycles": "node scripts/check-cycles.mjs",
    "check:push-auth": "node scripts/check-git-push-auth.mjs",
    "check:deploy-drift": "node scripts/check-deploy-drift.mjs",
    "check:csp-collector": "node scripts/check-csp-collector.mjs",
    "check:restarts": "node scripts/check-restarts.mjs",
    "check:blocked-empty": "node scripts/check-blocked-empty.mjs",
    "check:blocked-empty:controls": "node scripts/check-blocked-empty.mjs --selftest",
    "check:strands": "node scripts/check-blocked-empty.mjs",
    "check:strands:controls": "node scripts/check-blocked-empty.mjs --selftest",
    "check:phantom-rest": "node scripts/check-phantom-rest.mjs",
    "check:phantom-rest:controls": "node scripts/check-phantom-rest.mjs --selftest",
    "//check:routine-dispatch": "TRA-2331 — grades the claim check:phantom-rest MAKES. That check clears a leaf as monitored on `active` + `enabled` + a FUTURE `nextRunAt`, and ⛔ `nextRunAt` IS A PROMISE, NOT A RECORD: it is recomputed from the cron and moves forward whether or not the fire ever dispatched, so a routine whose every dispatch is failing reads byte-for-byte identical to a healthy one. The discriminator is the run history. Reads the LIST route only — ⛔ that route OMITS the `recentRuns` key entirely (absent, not []) so `(r.recentRuns || []).length === 0` brands the whole company never-dispatched; it DOES carry `lastRun`, which is the tail verdict in one call. Exit 0 clean / 1 per-routine findings / 2 FLEET (spans the roster — ONE platform condition, do NOT file a ticket per routine) / 3 blind. Zero graded rows is BLIND, never clean.",
    "check:routine-dispatch": "node scripts/check-routine-dispatch.mjs",
    "check:routine-dispatch:controls": "node scripts/check-routine-dispatch.mjs --selftest",
    "//check:spent-oneshot": "TRA-3008 — the complement of check:routine-dispatch. That one grades the DISPATCH TAIL (did the last fire run?); this one grades the FORWARD PROMISE. ⛔ A DATE-PINNED ONE-SHOT CRON HAS NO YEAR FIELD, so after its single fire the scheduler re-arms it ~365d out and the routine keeps reading as live scheduled coverage on every census — a SPENT one-shot is indistinguishable from an ARMED one, and its last run can be perfectly `completed`, so a tail verdict cannot reach it. Finds an `active` routine with an `enabled` schedule trigger whose `nextRunAt` is beyond the horizon (default 120d — wider than any legitimate quarterly cadence). ⛔ ARCHIVING DOES NOT DISABLE THE TRIGGER (measured on c0ca474e: status archived, enabled still true, nextRunAt still 2027) so `status==='active'` is the discriminator, not `enabled` — and archiving IS the remedy. ⛔ The routines route returns a BARE ARRAY: the first run of this census read `body.routines`, matched nothing, and reported 0 against a company that had 8. Exit 0 clean / 1 findings with coverage intact / 2 COVERAGE_LOST (a routine whose only arms are spent) / 3 blind. Zero graded rows is BLIND, never clean. ⛔ TRA-3017 — this check is armed DAILY (routine efd820ff) under prose that says `file ONE issue on exit != 0`, and two coverage-lost rows CANNOT be repaired by the agent running it (routine writes 403 across the assignee boundary; e1b97e28's assignee has DEPARTED, so NO agent can archive it — it needs a BOARD re-home). Left alone the daily fire mints a duplicate issue for the same rows every morning until someone retires the checker. `scripts/spent-oneshot-acknowledged.json` (HAND-EDITED ONLY) suppresses the EXIT CODE and nothing else — acknowledged rows are still printed IN FULL every run, every entry must name a live `trackedBy` issue plus a `reason` or it is IGNORED, and each is pinned to the cron+nextRunAt of EVERY spent arm on the row so a re-arm expires the pin by itself. New verdict ACKNOWLEDGED_ONLY = exit 0 “file nothing”, which is NOT “coverage is intact”. An unreadable ledger degrades to ZERO acks (more filing), never blanket suppression.",
    "check:spent-oneshot": "node scripts/check-spent-oneshot.mjs",
    "check:spent-oneshot:controls": "node scripts/check-spent-oneshot.mjs --selftest",
    "//check:data-dir": "TRA-2603 \u2014 frozen-baseline ratchet on the unguarded `process.env.DATA_DIR ?? join(__dirname,'..','data')` literal. `??` does not guard a present-but-BLANK value; resolveDataDir() adds the .trim(). Baseline is now ZERO known copies \u2014 TRA-2604 migrated all 38 \u2014 so any non-zero count is a REGRESSION, not backlog. HAND-EDITED ONLY; never regenerate it from the tree. Exit 1 drift / 2 instrument broken / 3 blind.",
    "check:data-dir": "node scripts/check-data-dir.mjs",
    "check:data-dir:controls": "node scripts/check-data-dir.mjs --selftest",
    "//check:continuity": "TRA-2634 \u2014 grade the level-continuity detector against the REAL stored report archive (both directions, every run) and cross-tab it against the deployed r>=2 session-move rule. Control rows are SCOPED PER BOX and only this HOST's are graded, so run BOTH: `pnpm check:continuity` (bqb1) and `HOST=http://localhost:4242 pnpm check:continuity`. Build @trading-app/shared first \u2014 it imports dist. Exit 0 pass / 1 control failed / 3 BLIND, and 3 IS A HOLD.",
    "check:continuity": "node scripts/tra2634-continuity-archive-check.mjs",
    "check:tra2361-prover": "node scripts/tra2372-monotonicity-prover-controls.mjs",
    "check:boot-set": "node --test scripts/lib/render-boot-set.test.mjs",
    "check:auth-secret-predicate": "node --test scripts/lib/auth-secret-predicate.test.mjs",
    "check:auth-secret-gate": "node scripts/tra2387-auth-secret-gate-check.mjs",
    "check:tra2399-vacuity": "node --test scripts/lib/tra2399-live-instrument-grade.test.mjs",
    "//check:tra2399-controls": "TRA-2399 AC3, END TO END: drives the real CLI over a real socket against a POPULATED book and an EMPTY one, and asserts exit 0 vs exit 4. Needs `pnpm build` first (it computes both payloads with the real gate out of dist/), which is why it is a named step and NOT in pretest \u2014 see the header of the script.",
    "check:tra2399-controls": "node scripts/lib/tra2399-populated-gate-server.mjs --self-test",
    "//tra2420:attribute": "TRA-2420 \u2014 rank DATA_DIR's per-trading-day GROWTH by writer off /api/health/storage/detail (ADMIN-gated since TRA-2599, so this needs TRADINGAI_ADMIN_TOKEN or --token=). Exits 2 NOT_DEPLOYED until the deploy carrying the `usage` block lands, which is the only answer live can give before then \u2014 hence the paired controls. Exit 5 NO_TOKEN is a MISSING/REJECTED token, deliberately not folded into 3 BLIND: BLIND reads as 'the box is down' and would send you to Render instead of to your shell.",
    "tra2420:attribute": "node scripts/tra2420-data-dir-attribution.mjs",
    "check:tra2420-controls": "node scripts/tra2420-data-dir-attribution.mjs --self-test",
    "//check:pnl-lag": "TRA-2630 AC3 \u2014 the T+1 options-credit lag regression guard (TRA-2629). Live-pull only: the defect is a DURABILITY seam, so an in-memory round trip passes while prod is broken. Partitions mode:live (exit 1, escalate) from demo (exit 2) and fails CLOSED to BLIND (exit 3) rather than reporting a green it could not measure. Exit 4 = LIVE CREDIT SHORTFALL (TRA-2635/TRA-2658), a SEPARATE axis that outranks demo lag on the exit code only \u2014 the `TRA-2630 AC2` delta verdict always prints ABOVE it and is what AC2 is graded on. AC2 is a DELTA (no NEW lag session on/after the fix-deploy date), never an absolute: the 18 pre-fix rows cannot clear retroactively.",
    "check:pnl-lag": "node scripts/check-pnl-options-lag.mjs",
    "check:pnl-lag:controls": "node scripts/check-pnl-options-lag.mjs --selftest",
    "//check:fix-live": "TRA-2644 \u2014 'is MY fix in the bytes being executed', which check:deploy-drift CANNOT answer. Ancestry of --commit against the live SHA, NOT drift: drift measures the whole company's merge rate against one box, so it is routinely non-zero on a weekday and a grading gate keyed to it never opens. Requires --commit (no default: a default is how a grader confirms the wrong fix). --window-start=<ISO> adds the arm a point-in-time SHA read cannot have \u2014 every build that held `live` across the window must contain the commit, because a rollback mid-session makes that session's EOD row a mixture of two writers. Exit 0 PRESENT / 1 ABSENT / 2 CONTAMINATED (window spans a build without the fix) / 3 BLIND. Fails closed: a SHALLOW checkout cannot distinguish 'not an ancestor' from 'unanswerable', so an unknown SHA exits BLIND, never ABSENT.",
    "check:fix-live": "node scripts/check-fix-live.mjs",
    "check:fix-live:controls": "node scripts/check-fix-live.mjs --selftest",
    "check:boot-arm": "node scripts/check-boot-arm.mjs",
    "check:boot-arm:controls": "node scripts/check-boot-arm.mjs --selftest",
    "check:theo-arb": "node scripts/check-theo-arb.mjs",
    "check:theo-arb:controls": "node scripts/check-theo-arb.mjs --selftest",
    "check:tra3010-gate-a": "node scripts/check-tra3010-gate-a.mjs",
    "check:tra3010-gate-a:controls": "node scripts/check-tra3010-gate-a-selftest.mjs",
    "check:mover-filter": "node scripts/tra2631-provenance-stamp-check.mjs",
    "check:mover-filter:controls": "node scripts/tra2631-provenance-stamp-check-selftest.mjs",
    "pretest": "node scripts/check-stale-js.mjs && node scripts/check-data-dir.mjs && node scripts/check-cycles.mjs && node --test scripts/lib/render-boot-set.test.mjs && node --test scripts/lib/auth-secret-predicate.test.mjs && node --test scripts/lib/tra2399-live-instrument-grade.test.mjs && node scripts/tra2387-auth-secret-gate-check.mjs && node scripts/check-theo-arb.mjs --selftest && node scripts/check-tra3010-gate-a-selftest.mjs && node scripts/tra2631-provenance-stamp-check-selftest.mjs && pnpm --filter @trading-app/shared build && pnpm --filter @trading-app/engine build && pnpm --filter @trading-app/backtest build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "concurrently": "^9.1.2",
    "eslint": "^9.0.0",
    "eslint-plugin-react-hooks": "^5.2.0",
    "globals": "^14.0.0",
    "typescript": "^5.4.5",
    "typescript-eslint": "^8.0.0"
  },
  "engines": {
    "node": "20.x",
    "pnpm": ">=9"
  },
  "pnpm": {
    "//": "TRA-1052 \u2014 .npmrc sets approve-builds=false, so pnpm skips dependency install/build scripts unless allowlisted here. better-sqlite3 is a native module that relies on prebuild-install (an install script) to fetch its prebuilt binary; WITHOUT this entry the binary is silently absent. TRA-1681 \u2014 this comment used to claim the server then 'crashes on require'. IT DOES NOT, and that wrong sentence is why the real risk looked self-announcing for months: sqlite.ts CATCHES the missing module, getStateDb() returns null, every caller falls back to in-memory, and the box boots and serves normally with the agent-spend cap NO LONGER DURABLE across restarts. The only signal is one log line, on a host that exposes no logs to a grader. `GET /api/health/durability` is now the readable verdict, and DURABILITY_POLICY=refuse is what makes it stop a boot. Listed explicitly so `pnpm install --frozen-lockfile` on Render fetches the prebuilt .node (no compilation, so no render-build OOM regression \u2014 TRA-1037/1038).",
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  }
}
