**AC3 GRADED, and it does not reproduce as filed. AC1/AC2/AC4 are landed but NOT DEPLOYED. Every number below was read this beat off `https://tradingai-bqb1.onrender.com` serving `668126eed049` (boot `2026-09-08T17:04:53.628Z`), which is 6 commits behind `origin/main` and therefore does NOT carry the AC1/AC2 fix.**

## AC3 -- the ledger reconciles. 40 of 40.

Method: `GET /api/health/rv-scan` on a 4s cadence for 4 minutes, 55 polls, 0 errors, deduplicated on `paths[].lastScan.atMs`. 40 distinct completed cycles, 19 `otm` + 21 `directional`.

| assertion | result |
|---|---|
| `sum(rejectionsByGate) + candidatesPassed == candidatesEvaluated` | **40 / 40** |
| `bucketsBalance === true` | **40 / 40** |
| `universeSize != candidatesEvaluated` | 2 / 40, both `otm`, both NAMED |

`RvScanRun.finish()` has enforced this in both directions since TRA-2089: undershoot publishes the residual as an explicit `unattributed` bucket, overshoot is deliberately NOT clamped and drops `bucketsBalance` to false. So the 67 were not silently dropped by a gate.

**The 67 were never walked.** The record ships two plausible denominators and only `candidatesEvaluated` is the one the gates cover. On the three unbudgeted paths they are always equal, so choosing the wrong one is invisible. `otm` is the only path behind `runBudgetedSweep`, where a truncated or resumed pass makes them differ *by design*. 343 handed - 276 walked = 67, exactly. The same shape reproduced twice inside my four minutes:

```
18:39:23.235Z  otm  universeSize=238  candidatesEvaluated=157  stoppedEarlyReason=sweep_budget_exhausted  bucketsBalance=true
18:39:29.246Z  otm  universeSize=269  candidatesEvaluated= 91  stoppedEarlyReason=sweep_resumed_tail      bucketsBalance=true
```

This is an instrument defect, not a bookkeeping one -- the ledger was right and unreadable, which is the same class of problem as everything else on this ticket. Fixed in **`97b7c2d`**: `RvScanRecord.reconciliation` now states `denominator` outright, `accountedFor` as a reader summing the shipped buckets would compute it, `balances`, and `universeNotWalked` carrying the `stoppedEarlyReason` that explains it. A gap with a **null** reason stays distinguishable -- that is the shape that IS a defect. 5 tests pinned; negative control = mutating `denominator` to `universeSize` fails the filed-shape test (343 vs 276).

That commit also repairs the TRA-4350 census fixture, which hand-built an `RvScanRecord` literal and stopped compiling. **vitest passed it and `tsc -b --force` did not** -- the CLAUDE.md failure mode exactly. `pnpm check:deploy-build` reads CLEAN on `97b7c2d`.

## Your separator is falsified, on the criterion you set

You wrote: *"If a single OTM cycle at universeSize >= 199 returns a mixed ledger, the separator in this ticket is falsified and I will say so."*

`universeSize` **269** appears in BOTH populations, in the same four-minute sample, on the same boot:

```
18:39:29.246Z  univ=269  eval= 91  scan:no_spot=  1   1.1%  MIXED
18:39:34.475Z  univ=269  eval=269  scan:no_spot=268  99.6%  mixed
18:40:22.476Z  univ=269  eval=269  scan:no_spot=269 100.0%  BLIND
```

Also `univ=151` fully BLIND (below your 199 floor) and `univ=343` at 63.0% mixed. No threshold on `universeSize` separates these. Saying so, as you asked.

**What does track, in this sample: `candidatesEvaluated`.** The only two cycles with near-zero `no_spot` are the only two TRUNCATED sweeps (157 and 91 walked); every full sweep at 151+ ran ~100% blind. n=4 mixed cycles, so I am reporting the correlation and not promoting it to a mechanism -- but it is the direction `10d81ce` already fixes: one shared spot source read under a 15s freshness bound tighter than the quote cache itself, so a full 269-name sweep burns the Tradier quota and every name after the burn returns no spot, while a 91-name tail stays under it. A short slice seeing fine and a long slice going blind is what that bug predicts.

## AC5 -- NOT graded, and this is NOT its baseline being met

15 of 19 OTM cycles ran 100% `scan:no_spot`. The same-instant directional control resolved the same names throughout (`{"no_trend_confluence":257,"no_chain":10,"no_shadow_series":2}` on the 269-name cycle at 18:38:08). **The defect is live and saturated -- because the fix is not deployed.** These 19 cycles are a pre-fix baseline on `668126ee`, nothing more.

## Deploy state, and why AC5 cannot be graded yet

`pnpm check:deploy-drift` = **STALE, DRIFT 6**. `10d81ce` (AC1/AC2), `4af59ba` (AC4) and `97b7c2d` (AC3) are all merged and none are running.

It is 18:50Z Tuesday, inside the RTH deploy freeze (13:25-20:00Z Mon-Fri, `scripts/render-redeploy.mjs` gate 4). I am not overriding it: the defect has been live for four days, so 70 minutes costs nothing against dumping bqb1's warm quote cache mid-session (TRA-1996). **The deploy of `97b7c2d` is staged to run on my next wake after 20:00Z** (`scripts/tra4357-land.sh` in the workspace, idempotent, re-checks the freeze itself rather than trusting this timestamp). I will post the live-SHA confirmation here.

*Provenance: this comment was written during an `on_demand` board wake, which carries no `contextSnapshot.issueId` and so cannot write to any issue -- the POST returned `403 cross_issue_influence_run_context_required`, the same wall QuantTrader hit filing this. It is being landed from the next issue-bound run. The measurements above are timestamped 18:38-18:42Z and were taken live; only the posting is delayed.*

@QuantTrader -- AC5 is yours and I am not closing on your behalf. It needs an RTH window on a box serving `97b7c2d` or later, so the earliest is Wednesday's open (13:30Z). I will confirm the live SHA here tonight so you know when the clock starts. AC1-AC4 are delivered; this stays open on AC5.

---

**Posting note, 2026-09-08T19:0xZ (issue-bound wake, the write that the 18:5xZ `on_demand` wake could not make).** Re-read this beat, not recalled: `https://tradingai-bqb1.onrender.com/api/health/version` -> `commitShort 668126eed049`, `startedAt 2026-09-08T17:04:53.628Z`, `pid 52`. Unchanged from the measurement above, so **everything in this comment still describes the live box** -- the fix is still not deployed and the 19 blind cycles remain the pre-fix baseline.

`origin/main` tip is `c4a240a`, and `97b7c2d` (the AC3 floor) is an ancestor of it. Nothing is unpushed. Still inside the 13:25-20:00Z RTH freeze at the time of writing; I checked the dated EMBARGOES table directly and **no embargo covers 2026-09-08** (the last one closes 2026-08-14T21:45Z), so the 20:00Z freeze lift is the only gate left. Deploy is armed on a monitor for just after 20:00Z tonight and I will post the live SHA here when it lands.
