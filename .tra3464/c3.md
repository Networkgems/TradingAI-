## Disposition: `in_review` behind an armed + verified monitor at **2026-08-14T20:45:00Z**

Monitor read back byte-exact off a re-GET, `scheduledBy: assignee`, status unchanged:

```
STEP 2: prove partitionHolds NON-VACUOUSLY. 1) check:deploy-drift; require 1d7f180d live off
/api/health/exit-cadence .build. Not live -> RE-ARM, do NOT grade. 2) build.startedAt MUST be
< 2026-08-14T13:30Z or the box never spanned RTH -> RE-ARM; a post-close boot passes vacuously, all
terms 0. 3) Per book assert window=='rth', samples>0, partitionHolds===true, lifetime.tickExitRegionMs
.samples == samples+boundary+closed+bootRegionsExcluded. Then Step 4: delete $.tickExitRegionMs.
```

The liveness leg and the span leg are both **re-arm** branches, not grade branches. Your own instruction is the reason: *"a vacuous pass is not a pass; say which it was."* The only way to guarantee I never say the wrong one is to make the grader unable to reach a verdict without first proving the box could have produced one.

---

## Step 3 is BLOCKED ON A 403, and I am not recording it as done

Step 3 orders a comment on TRA-2305 and TRA-2268 naming the new path before the field moves. **I cannot post to either.** All three of TRA-2305, TRA-2268 and TRA-2698 return:

```
403 {"error":"Issue is outside this actor's authorization boundary"}
```

I have filed **TRA-3583** (child of this ticket, assigned to CTO, `todo`) carrying the notice verbatim plus the `368ce26f` edit instructions, and asking for it to be pasted onto TRA-2305/TRA-2268 or for a route that works.

Recording it that way rather than as satisfied, because **an advance notice that never reaches the thread it is warning is the same as no notice** -- and the whole value of Step 3 is that it lands while the field has not moved yet. It still has not: `DRIFT = 5 -> STALE`, live `4cac8b70` / pid 75. That window closes at the next bqb1 deploy, expected ~20:15Z tonight.

## Why this is `in_review` and not `blocked`

TRA-3583 does not gate Step 2. The deploy rides a scheduled fire, the monitor re-derives the live SHA itself, and neither depends on CTO acting on TRA-3583. Resting `blocked` on it would be a first-class blocker claim I cannot support, and would park the real work behind a routing errand.

## What remains, in order

1. **Step 2** -- the monitor above. Non-vacuous `partitionHolds`, stated as which.
2. **Step 4** -- delete `$.tickExitRegionMs` in a separate commit, let `tsc` enumerate what is left, and stop-and-report anything it names that is not on the list. I already know `tsc` will name zero, because the only two consumers outside its reach (`EXIT_CADENCE_NOT_DIFFERENCEABLE`'s frozen string list, and `tra3443-exit-region-taxonomy.mjs`) are both migrated in `1d7f180d` -- but that is a prediction and the run is the evidence.
3. Then AC5 and AC6 are met, AC3 is met, and TRA-2698 has everything it asked for.

Nothing here asks you to close TRA-2698 yet. Step 1 is shipped and unit-proven; it is not deployed and it is not measured.
