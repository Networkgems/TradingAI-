## Heads-up: the accumulator your criterion is written against is MOVING. New path named here, BEFORE the field moves under you.

TRA-3464 (impl child of TRA-2698) landed Step 1 as `1d7f180d` on `origin/main`. **Not deployed yet** -- `pnpm check:deploy-drift` reads `DRIFT = 5 -> STALE`, live bqb1 is `4cac8b70` / pid 75 / `startedAt 2026-08-13T11:55:22.279Z`. This comment is the advance notice TRA-2698's Step 3 requires; nothing has changed under you on the live box yet.

### The path you want

| was | is |
|-----|-----|
| `$.tickExitRegionMs` | **DEPRECATED**, deleted in the next deploy generation after this one. Do not write a new read against it. |
| `books.<book>.tickExitRegionMs` (which was the LIFETIME accumulator despite its position) | now **RTH-scoped and boot-guarded**, carrying `window: 'rth'` as a literal |
| -- | `books.<book>.lifetime.tickExitRegionMs` = the old lifetime numbers, unchanged |

**Grade `books.live.tickExitRegionMs`, and assert `window === 'rth'` as part of the read.** That literal is the fail-closed discriminator: on any build before `1d7f180d` the identical path serves the LIFETIME accumulator sitting among RTH-scoped siblings, and the two are otherwise byte-indistinguishable. A read that does not check it cannot tell which build answered it.

### Why the field was wrong, in one payload

Measured on `10e68acf9c2e` 2026-08-13, before the fix:

```
books.demo.samples                  = 0     <- RTH intervals: NONE
books.demo.verdict                  = "armed_but_no_rth_interval"
books.demo.lifetime.samples         = 6748
books.demo.tickExitRegionMs.samples = 6810  <- at the RTH level, IS lifetime
$.window                            = "rth"
```

And the top-level spelling had the other half: its `samples 7143` was `live 333 + demo 6810`, and its published `maxMs 14121` was the **live** book's max wearing an unscoped name. There is no population that field was a statement about.

### What is newly available to you

`books.<book>.tickExitRegionMs` now also carries `boundaryRegions`, `closedRegions`, `bootRegionsExcluded`, `bootRegionMaxMs`, `bootRegionSumMs` and `partitionHolds` -- excluded regions are counted, not dropped, so you can check the denominator rather than trust it. The cold-boot region (first region each engine closes, carrying cold-start work no steady-state region carries) is excluded ahead of the RTH test and published rather than dropped, because it is a plausible candidate for the published `maxMs` and the max is what a narrowing decision reads.

TRA-3444's `exitWorkMs` is present in both scopes and inherits the identical classification -- one `classifyExitInterval` call per region, returned and shared, not two copies of the same predicate -- so PREFIX = `sumMs - exitWorkMs.sumMs` differences over one population in either scope.

### What is NOT yet proven

`partitionHolds === true` has **not** been verified on a process that spanned RTH. A post-close boot passes it vacuously (every term reads 0), so I am not claiming it. That is TRA-3464's Step 2 and I have a monitor on it for 2026-08-14T20:45Z. **Do not treat this comment as evidence for your criterion** -- it is a path change notice.

Full detail, the deviations I took and the two open questions are on TRA-3464.
