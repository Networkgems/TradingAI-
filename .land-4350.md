## AC1 GRADED PASS. Terminal read as the monitor ordered; closing, not re-arming.

Read at 2026-09-09T00:0xZ on live pin **c7b452c0fc92** / pid **76** / `startedAt`
**2026-09-08T20:49:38.003Z**. `668126eed049` has since been superseded by `c7b452c0` (a later
post-close train); `censusByEtDay` and `armNote` are both still served, so the instrument this AC
grades is intact across that boot.

**CLASS B lateness note.** This read landed ~4h after the monitor fired. Per the WINDOW GUARD
classifier, `censusByEtDay` is retained on disk 30d and accumulates monotonically across boots, so
a late read is a strict **superset** of the in-window one. The verdict is VALID, recorded with its
lateness beside it -- not a NOT RUN.

### The cell

`/api/health/rv-scan` -> `censusByEtDay[etDay=2026-09-08]`, `path=otm`, `accountClass=desk`,
`mode=live`:

```
state                 ran_and_declined
books                 3        boots  6      scans  232
candidatesEvaluated   112594
candidatesPassed      0        opensPlaced  0
rejectionsByGate      scan:no_spot            103234
                      scan:no_expirations       4848
                      contract_floor_delta      1358
                      entry_window_closed       1222
                      scan:no_chain              652
                      recent_duplicate           561
                      no_candidates              449
                      contract_floor_premium     145
                      no_in_band_strike          125
```

Against the three PASS conditions in the monitor note:

1. `state == ran_and_declined` -- **PASS**
2. `candidatesEvaluated > 0` (112594) -- **PASS**
3. `rejectionsByGate` has `entry_window_closed` (1222) -- **PASS**

**AC1 passes.** The discrimination the ticket was filed for now works: the route separates a
drought (`ran_and_declined`, with the gate named) from a writer fault (`passed_but_no_open`) from
an empty universe (`ran_unfed`), where all three previously read as the same zero. The cell is
present rather than ABSENT, so this is a reading and not the absence of one.

This closes the "options tape is DARK and nothing can say why" question: the tape is dark because
the OTM pass **ran 232 times over 112594 candidates and declined every one**, not because a writer
dropped opens and not because the pass never reached a book.

### One thing I am NOT claiming, flagged for TRA-4357

`scan:no_spot` is **103234 / 112594 = 91.7%** of desk/live OTM rejections -- the TRA-4357 condition,
still dominant. But `blindScans` reads **0** with `scans` 232, and `censusNote` warns that
`blindScans`/`universeSum` are 0 on lines written **before 2026-09-08**, meaning NOT MEASURED rather
than "none". This cell spans a day the counter shipped into, so I will not read that 0 either way.
TRA-4357 owns that discriminator and carries an armed monitor for 2026-09-09T14:30Z; leaving it
there rather than double-grading it from here.

Monitor said "close TRA-4350 either way, do NOT re-arm." AC1 passed. -> `done`.
