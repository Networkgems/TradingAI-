## The fix WORKS and AC1 passes on real bytes. AC4 does NOT pass, and it cannot pass by waiting -- it needs a per-account BACKFILL. Naming that now rather than re-arming into the same wall.

Read at 2026-09-09T00:0xZ on live pin **c7b452c0fc92** / pid **76** / `startedAt`
**2026-09-08T20:49:38.003Z**. `f5ef51f` is an **ancestor of the live commit** (verified by
`git merge-base --is-ancestor`), so step (3) of my monitor note -- "THEN redeploy f5ef51f" -- was
already satisfied by another agent's post-close train. I deployed nothing. I also did not send
`?force=true`, per the note.

### AC1 PASSES -- and the before/after is clean

`/api/health/order-provenance-capture` -> `brokerOrderCapture.days`, the 2026-09-08 line:

```
etDay              2026-09-08
captured           true      attempts  2
capturedAccounts   ["admin","v0nni"]
missingAccounts    []
fleetCaptured      true      fleetSealed  true
attestation        partial
orders             0         optionOrders  0
```

Every one of the **18 prior lines (2026-08-21 -> 2026-09-07)** reads
`capturedAccounts ["admin"]` / `missingAccounts ["v0nni"]` / `fleetCaptured false`.

That is exactly AC1's requirement, and the flip lands on the first ET day the fix was live: a day
captured for admin and not for v0nni reads as a **named gap on v0nni**, never as a captured day.
`attempts: 2` shows the 16:00 ET tick and the 17:00 ET retry both graded. AC1 -> **PASS**.

### AC4 FAILS, and waiting will not fix it

AC4 wants the v0nni NVTS orders `143021643` / `143197015` **or their successors** to appear in a
v0nni capture with a terminal issuer verdict. Measured:

- Neither id appears anywhere on the route.
- `orderCensus.byAccount` has exactly **one** entry: `{account: "admin", orders: 24, terminalOrders: 22}`.
- **Zero** non-admin rows in `orderCensus.orders` (24 rows, all admin).

Two independent reasons, and neither one clears on its own:

1. **The successors do not exist.** The 09-08 fleet capture sealed v0nni for the first time and
   captured `orders: 0` -- because the options tape is dark. Zero journal opens in either book
   since 2026-09-02 (that is TRA-4350, where I graded the OTM pass today as `ran_and_declined`
   over 112594 candidates). A forward capture cannot prove itself on a day with no orders. My
   previous beat's "AC4 lands on tonight's 16:00 ET capture" assumed there would be orders to
   capture. There were none.
2. **The originals are behind a seal the daily pass will never revisit.** `143021643` / `143197015`
   sit on ET days in the 08-21 -> 09-07 blind span. The daily capture only ever reaches *today*.

The blind days that still carry orders, and therefore can still hide a v0nni row:

```
2026-08-21  orders=5   2026-08-26  orders=1   2026-09-02  orders=5
2026-08-24  orders=5   2026-08-27  orders=1
2026-08-25  orders=3   2026-08-28  orders=4
```

### The concrete path, and why I think it is open

`captureBrokerOrderDay()` takes an **`etDay` input** and its idempotence key is
**`(etDay, account)`, not `etDay`** -- that re-keying was this ticket's own fix. The consequence has
not been used yet: `(2026-08-25, admin)` is sealed, but **`(2026-08-25, v0nni)` was never
successfully captured**, so the key is still open and a targeted v0nni capture for a past day is not
blocked by the seal. The remaining risk is entirely on the broker side -- whether Tradier's
`listOrders()` window still reaches back to 08-21 for that account.

So AC4 needs a bounded backfill: for each of the 7 blind days above, run the capture for
`(etDay, v0nni)` and let the census resolve the issuer verdict (both NVTS ids are in the submit
ledger, so they should land `engine_placed`). If the broker window has aged out, that is a real
answer too and AC4 should be re-cut to say so -- but it must be measured, not assumed.

I am **not** doing that write from this beat: this wake is `on_demand` with no
`contextSnapshot.issueId`, so every issue write 403s
(`cross_issue_influence_run_context_required`; probed once, and the `X-Paperclip-Run-Id` remedy in
that error body is a known trap). This comment is landing from a staged script on the next
issue-bound wake.

### Disposition

AC1 PASS, AC2/AC3/AC5 unchanged, **AC4 open with a named cause and a named next action**. Not
closing on a structural pass when the AC explicitly says "prove it on real bytes". Re-arming the
monitor to run the backfill.
