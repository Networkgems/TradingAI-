## Acceptance item 4 is UNGRADABLE: all three drain arms are still board-paused. Converting this from a spent monitor to a first-class blocker on TRA-4229.

Measured 2026-09-09T00:0xZ against the routine records, not against a comment:

```
9cff4e6f-8959-4b40-b457-4f01d5e62923  CTO ARM -- twice-daily strand DRAIN   status=paused
db971fb9-b8ff-41df-a997-b8776aaae579  CFO ARM -- twice-daily strand DRAIN   status=paused
02abcd1b-7075-47d3-b333-567557ded6c1  CEO ARM -- twice-daily strand DRAIN   status=paused
```

All three still `paused`, all three `catchUpPolicy: skip_missed`. So there has been **no post-fix
fire on any arm**, and acceptance item 4 -- "grade the FIRST post-fix strand-drain fire exit code
(fix `7465ba6d`)" -- has no subject to grade. This is the absence of a reading, not a green one.

`skip_missed` matters here: the slots these arms have missed while paused are **not replayed**. When
the board un-pauses, the first fire is the next scheduled slot, not a catch-up burst.

### Why this stops being a monitor

My monitor note said "if arms are still paused, re-arm this monitor." I am not doing that, because
re-arming would be the third consecutive wake into an unchanged wall, and it misrepresents the
state: this is not a timed wait on a clock I am watching, it is a wait on **a decision another
party owes**.

The arms are paused by the 2026-08-27 fleet halt. That decision is **TRA-4229**, which carries
board card `c53c063e` (`ask_user_questions`, pending since 2026-09-01T02:34, **no expiry**). Until
that card is answered the arms stay paused and item 4 stays ungradable.

That is a first-class blocker with a named owner and a named unblock action, so this rests
`blocked` **with `blockedBy: [TRA-4229]`** -- not `blocked` with an empty `blockedBy`, which is the
strand condition itself, and not a fourth one-shot monitor.

**Unblock owner:** board. **Unblock action:** answer card `c53c063e` on TRA-4229 (re-arm the 41
paused routines + 2 agent seats, or rule "keep quiet"). On a re-arm, this issue wakes via the
blocker edge and item 4 grades on the first fire that lands -- expected `0 DRAINED`, or `0` with
VACUOUS and no refusals; a `2` on a refusal reopens `7465ba6d`.

The rest of the ticket (the non-ASCII `originSentence()` fix itself) is shipped and unchanged; item
4 is the only open acceptance item.
