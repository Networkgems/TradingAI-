## AC4 still ungradeable -- but the reason CHANGED today, and the staged `blocked` rest is now illegal

Reached from the TRA-4301 stalled-review sweep leaf TRA-4431. This row's one-shot monitor was
consumed by the **2026-09-08T12:51:13.909Z** scheduler burst (TRA-4141, third dated instance,
already folded in via TRA-4391) without delivering a wake -- `attemptCount: 2`,
`monitorNextCheckAt: null`, `executionState.monitor.status: "triggered"`. Spent, so nothing was
going to wake this row again.

### The measurement (2026-09-09 ~14:1xZ)

All three per-seat strand-DRAIN arms are **still `status: paused`**, and none has fired since the
fix:

| arm | seat | routine status | last run | last run status |
|---|---|---|---|---|
| `9cff4e6f` | CTO (mine) | paused | 2026-08-28T01:17:22.037Z | failed |
| `02abcd1b` | CEO | paused | 2026-08-28T01:17:23.574Z | failed |
| `db971fb9` | CFO | paused | 2026-08-28T01:17:24.782Z | failed |

The fix is `7465ba6d` (landed 2026-09-01). Every fire above predates it. So there is still **no
post-fix fire to grade**, which is the same conclusion as the 09-03 monitor beat -- but it now
rests on a decided question rather than an open one.

Triggers all still read `enabled: true` with stale `nextRunAt` (`9cff4e6f` -> 2026-09-05T00:30Z,
`02abcd1b` -> 2026-09-05T00:45Z, `db971fb9` -> 2026-08-28T13:00Z). That is the known
paused-routine pattern: the gate is `routine.status`, not the trigger's `enabled` flag or its
`nextRunAt`. An advancing `nextRunAt` is not evidence a routine is live.

### What changed: the board answered, and the answer is "stay paused"

**TRA-4229 closed `done` at 2026-09-09T13:32:18Z.** The ruling was posted at 14:07:24Z by CEO, off
board interaction `9029c021` answered 13:44Z, routed through the TRA-4426 cleanup:

> Board ruling via TRA-4426 cleanup: keep ALL 41 routines and both agent seats PAUSED. No re-arm
> during the stabilization freeze (through 09-18).

So the wait is no longer indefinite -- it has a date. I am not re-arming `9cff4e6f` unilaterally
even though it is mine and I could: the ruling is fleet-wide and explicitly says "recorded for the
next person who considers re-arming." That is me, today, and the answer is no.

### Why this is NOT resting `blocked` -- a correction to the staged disposition

`scripts/tra-20260909-land.mjs` staged this row as `blocked` on TRA-4229, graded during the
2026-09-09T00:0xZ `on_demand` beat when TRA-4229 was still open. That PATCH ran today and was
**correctly refused**:

```
HTTP 422 {"error":"Entering blocked requires unresolved blockers, a pending interaction/approval,
          or unblockDescriptor"}
```

TRA-4229 went `done` at 13:32Z, roughly 40 minutes before the land ran. A blocker edge onto a
resolved issue is exactly the empty-`blockedBy` strand condition the TRA-3541 drain arms exist to
repair, so the 422 is the platform being right and the staged grade being stale. Worth stating
plainly because it is the failure mode of stage-and-land in general: **a staged disposition carries
the world-state of the beat that graded it, and a blocker edge is the part of that state most
likely to have moved.** The other three rows in the batch (TRA-4241, TRA-4350, TRA-4009) landed
clean; only the one asserting a relationship to another live row went stale. A close-status check
on every `blockedByIssueIds` target immediately before the PATCH would have caught it, and I would
rather the script had refused itself than been refused.

### Disposition: re-arm the monitor for after the freeze

This is a timed wait with a named date, so it rests in a monitor per TRA-4073 -- disposition (1),
the same one I am asking other owners to use in today's sweep. Re-armed for
**2026-09-21T14:00:00Z** (Monday 10:00 ET): the freeze runs through Friday 09-18, and by Monday
morning each arm has had at least one twice-daily slot (CTO 08:30 ET, CEO 08:45 ET, CFO 09:00 ET)
in which to fire if the fleet was re-armed on schedule.

On that wake: if any arm has fired post-`7465ba6d`, grade the exit code -- **0 DRAINED**, or
**VACUOUS with zero refusals**, is the pass; an exit **2** on a refusal reopens this. If the fleet
is still paused, check whether the freeze was extended before re-arming again, and if it was
extended without a date, escalate rather than loop -- three consecutive re-arms against the same
unchanged cause is a signal that the monitor is standing in for a decision nobody is making.
