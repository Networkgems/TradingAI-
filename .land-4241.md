## AC4 GRADED PASS on the live build. All five ACs are now satisfied; closing.

Read at 2026-09-09T00:0xZ against the pin this AC asks for:
`build.commitShort` **c7b452c0fc92** / **pid 76** / `startedAt` **2026-09-08T20:49:38.003Z**
(`/api/health/version`, `commitSource: git`, `uptimeSec` 11764).

### The deploy this monitor ordered had already happened

My monitor note said "on wake: deploy 30ad9b0 to bqb1". I did **not** deploy. `30ad9b0` is an
**ancestor of the live commit c7b452c0** -- another agent's post-close train carried it while the
RTH freeze was blocking mine. Verified by `git merge-base --is-ancestor 30ad9b0 c7b452c0` before
touching anything, which is the whole reason a deploy order is graded on ancestry and not on
"did I personally push the button". Live is 3 commits behind `origin/main` (1fa511f, 978e354,
5602d74); none of them touch this path.

So there is no build-log banner to grade: the banner check existed to prove MY deploy ran, and my
deploy correctly never ran.

### AC4: `closeSupersedes` off `/api/health/option-journal`

```
{ "total": 3, "dropped": 0, "applied": 1, "refused": 2, "live": 3 }
```

**total 3 / applied 1 / refused 2** -- exactly the boot-replay shape predicted in the monitor note,
and `refused >= 1` as AC4 requires.

The refusal that matters, on `closeSupersedes.recent`:

```
id       8a849902-8dec-409a-9498-d1411e97f26f
reason   engine_close_on_already_closed_row
refusal  witnessed_other_order
applied  false
```

### AC2 + AC3 re-confirmed from the row itself, not from the ring

AC3 is satisfied -- under `supersededCloses` / `refusedCloseSupersedes`, not the literal
`supersededFrom` the AC offered as an example. Off `?rows=all`, the canonical RIG row:

```
id                       8a849902  (RIG260925C00006000, live)
exitReason               sl_otm_premium_pct
brokerOrderId            143048620
realizedPnlUsd           -15.24
refusedCloseSupersedes[0]:
  refusal                witnessed_other_order
  refusedAt              1787751931278
  incoming exitReason    profit_lock
  incoming brokerOrderId 143384264      <-- a DIFFERENT broker order
  incoming realizedPnl   -7.00
  retainedCloseTs        1787584555327
  retainedExitReason     sl_otm_premium_pct
  retainedRealizedPnlUsd -15.24
  retainedBrokerOrderId  143048620
```

This is AC2's rule visible as data: the incoming close keyed to broker order **143384264** while
the row was witnessed on **143048620**, so it REFUSED rather than applied. Keyed on the broker
order, never on the clock (the TRA-4082 rule).

And it is AC3's property visible as data: `sl_otm_premium_pct` / `143048620` / `-15.24` all survive
**on the row**, with the `retained*` quartet making the grade re-derivable without the bounded
`closeSupersedes.recent` ring. The three tokens the monitor note told me to re-check are present on
`?rows=all`.

### Ledger for the other ACs

- **AC1** answered 2026-09-08T19:30 (broker tape).
- **AC2 / AC3** shipped to `main` in `30ad9b0`, with the positive-control test
  `tra4241-supersede-refuses-witnessed-other-order.test.ts` built from this row's two payloads.
- **AC5** posted, and **answered** by QuantTrader 2026-09-08T19:37: reading **(1) STANDS**.

AC4 was the only one outstanding and it passes. -> `done`.
