# Root package.json — script rationale notes

Moved here verbatim from the `//` pseudo-script keys in the root `package.json` under **TRA-4482** item 2 (audit TRA-4473, board comment pinned 5cedba5). Each key still exists in `package.json` beside its real command, holding a one-line headline plus a pointer to its section here. The text below is the point-of-use operator rationale, unedited; CLAUDE.md independently carries the critical warnings.

Source: `origin/main` = `d5b44539`, moved 2026-09-23. **When you add a new `//` key, keep the in-file value to one line and put the body here.**

⚠️ `"//check:slot-loss"` appeared **twice** in the source file (a duplicate JSON key — every JSON parser silently discards the first entry, so the first text below was unreachable to tooling and survived only as bytes). Both texts are preserved here; the key now appears once in `package.json`.

## `scripts.//build`

~~~~text
TRA-3720 — this read `pnpm --filter desktop tauri build` and had NEVER run: `pnpm --filter <pkg> <name>` runs a SCRIPT called <name>, and `desktop` has no `tauri` script (its `build`/`dev` invoke the tauri BINARY). So it died in pnpm with ERR_PNPM_RECURSIVE_RUN_NO_SCRIPT, exit 1, before any compiler started — while CLAUDE.md prescribed it as the safe `tsc -b` verify and four call sites (`//check:tra2399-controls`, `check-nonsession-rows.mjs`, `lib/tra2399-populated-gate-server.mjs`, `check-stale-js.mjs`) already read `pnpm build` as 'compile the packages to dist/'. Pointed at that meaning, which is the one the repo already had. The tauri desktop build is NOT vestigial (`apps/desktop/src-tauri/` is real) — it moved to `desktop:build`/`desktop:dev`, with the same script-vs-binary bug fixed. Deploy is unaffected: render.yaml runs `render-build`, and check-deploy-build.mjs grades only [render-build, web:build].
~~~~

## `scripts.//web:build`

~~~~text
TRA-3720 — the filter was single-quoted (`'./packages/**'`). pnpm runs scripts through cmd.exe on Windows, where `'` is NOT a quote character, so pnpm received the quotes as part of the pattern, matched NOTHING, and exited 0: on any Windows checkout this segment compiled ZERO packages and reported SUCCESS. Linux (Render) uses sh and was always fine, so no deploy was ever affected — but the local pre-push check:deploy-build gate executes these segments verbatim, and it runs here. Double quotes are honoured by both shells; `@trading-app/*` selects the same five packages the glob did (agents included) — measured `Scope: 5 of 7`.
~~~~

## `scripts.//check:deploy-origin`

~~~~text
TRA-4789 — every deploy gate we own lives INSIDE render-redeploy.mjs, which an operator can simply decline to run. Three `trigger: manual` dashboard deploys reached bqb1 on 2026-09-13 (04:04Z, 04:51Z, 12:49Z, all b22a9999) having run NO gate, and the gate set read 'in force' identically throughout. Option (a) — take the dashboard button away with Render permissions — was MEASURED and is unavailable: the workspace has exactly one member and they are its ADMIN owner, so there is no seat to restrict. This is (b). Classification is `deploy.trigger` (only `api` is sanctioned; an UNKNOWN value reads BLIND, never clean); attribution is `deploy_started.details.trigger` off the events route, which DOES carry an actor — correcting TRA-4384 §2. Legs that disagree read BLIND. Needs RENDER_API_KEY. 0 CLEAN · 1 BYPASS · 2 usage · 3 BLIND; BLIND > BYPASS > CLEAN.
~~~~

## `scripts.//tra4158:heap-series:controls`

~~~~text
TRA-4158 — controls for the RTH-HOURS ruler and the restated AC1 gate. AC1 as filed asks for two heap reads ~+1h/+8h of UPTIME apart; measured over 26 deaths, rss-at-death correlates r=0.560 with wall-clock uptime and r=0.839 with RTH-hours, and a real 68.16h WEEKEND boot carries 2.66 RTH-hours — it satisfies AC1-as-filed while showing no growth, a STRUCTURAL false negative. The gate is therefore >= 4 RTH-hours apart on ONE boot, and `overnight-pair-8h` is the control that must FAIL it. The calendar models weekends, the 2026 US market holidays and the three half-days; anything it gets wrong OVERSTATES exposure, i.e. fails PERMISSIVE, so the report says so out loud past 2026-12-31.
~~~~

## `scripts.//check:new-supersession`

~~~~text
TRA-3867 — grade the day-cell/journal supersession axis on LIVE bqb1. The raw `journalDayCellAgreementOk` is false PERMANENTLY under the TRA-3864 (b) freeze ruling (TRA-2079 refuses to move a non-zero cell), so it is not gateable; this grades `journalDayCellNoNewSupersessionOk`, which subtracts the pinned+published acknowledged set. It replays the deployed fold with a synthetic 4th divergence and requires the verdict to FLIP — a green that cannot go red is the old vacuous pass wearing a new name. Needs TRADING_ADMIN_USERNAME/PASSWORD. 0 PASS · 1 FAIL · 3 BLIND.
~~~~

## `scripts.//check:deploy-gates`

~~~~text
TRA-2325/TRA-3699 — the discrimination suite for render-redeploy.mjs's five gates (RTH freeze, dated embargo, commit hold, stale-pin rollback, ancestry grader). Offline by default and now in pretest, because TRA-3699 shipped a fail-OPEN in gate 2 that no test would have caught: every commit-hold case INJECTS `carries`, so none of them reached the real git predicate, and the suite stayed green while a shallow graft silently lifted a hold on the live-money host. Every REFUSE case is paired with a PROCEED case differing by one variable, and the run FAILS if any verdict is never reached — a one-sided suite rubber-stamps a jammed gate. `--live` adds the network arm (real origin/main tip + bqb1's served sha); it is opt-in and NOT in pretest. TRA-4535 adds the CADENCE CEILING (exit 10) arms: a REPLAY of bqb1's real Render deploy history for 09-08..09-10 against the shipped ceiling row (the 09-09 window's 9 advances: row 1 PROCEEDS, rows 2-9 REFUSE), one-variable pairs, and the shipped main() spawned against the read-only stub.
~~~~

## `scripts.//check:arg-guard`

~~~~text
TRA-4420 — the discrimination suite for the ARGUMENT GUARD in render-redeploy.mjs (Gate -2). Every flag in that script was looked up POSITIVELY, so an argument it did not recognise was silently ignored and the run proceeded as a real deploy of the branch TIP: on 2026-09-09 `--help` printed no usage and triggered dep-dagcbqp5efls73ac5d40 on bqb1. `--help` is the harmless instance; the expensive ones are `--commmit=<sha>` and `--commit <sha>`, which read as 'no --commit given' and shipped the tip INSTEAD OF the sha the operator named. The load-bearing arms are REAL INVOCATIONS of the shipped bytes against the read-only Render stub, not an injected table — the defect was never a wrong predicate, it was the absence of a call site, and a table-only suite stays green if main() stops calling the guard. ARM 0 is the negative control: it reconstitutes a187fc14's bytes and asserts they STILL reach POST /deploys, so this suite cannot go green because the repro stopped biting. In pretest.
~~~~

## `scripts.//check:deploy-gates:shallow-repro`

~~~~text
TRA-3699 — the other half of the control, which no injected table can reach: that the real git path on a real GRAFTED repository actually produces the input that table grades. Builds the repro from scratch (clone --depth=1 via file://, THEN fetch --depth=1 each sha, so the OBJECT is present and the PATH between them is cut) and runs the shipped bytes of render-redeploy.mjs inside it. ⛔ A plain --depth=N clone is NOT the repro — there the object is simply absent and the existing gitHasCommit screen already exits BLIND for the right reason, so the arm would pass without the fix. Arm 1b re-runs the PRE-FIX bytes pinned by rev and asserts they still read CLEAR, so a fix arm cannot go green because the repro stopped biting. NOT in pretest on purpose: it needs a COMPLETE clone (it exits 2 on a shallow one), and pretest is exactly where a shallow checkout is likely.
~~~~

## `scripts.//check:ancestry-graders:shallow-repro`

~~~~text
TRA-3721 — the FAIL-OPEN half of the TRA-3699 ancestry sweep, on the SAME grafted state (the builder is shared: scripts/lib/shallow-graft-repro.mjs). Two graders collapsed an unreadable answer into a permission: tra2342-interlock-live-check.mjs's `--self-test` NEGATIVE CONTROL, which on a graft printed `SELF-TEST OK` without having discriminated anything — and that sentence is what licenses the checker's PASS on the `TRADIER_ENV` write and on arming live crypto — and tra2306-ceiling-grade.mjs's second TRA-2355 detector, which guarded every exit status except `1`, the one the graft lands on. Controlled in BOTH directions: every grafted arm has a complete-clone twin that must still ANSWER, because after a change that turns negatives into BLIND, `refuses everything` is the obvious way to fail. Pre-fix arms are pinned by rev (12305eba), so a fix arm cannot go green because the repro stopped biting. NOT in pretest, same reason as the TRA-3699 one: it needs a COMPLETE clone.
~~~~

## `scripts.//check:false-red-graders:shallow-repro`

~~~~text
TRA-3722 — the FAIL-CLOSED half of the TRA-3699 ancestry sweep, on the SAME grafted state (shared builder: scripts/lib/shallow-graft-repro.mjs). Four graders condemned a build that CARRIES the fix, because `merge-base --is-ancestor` exits 1 both for a genuine non-ancestor and for a shallow graft: check-rv-sleeve-live.mjs leg 4 (dead(), exit 1, 'no RV outcome row from this build is real'), tra2407-fold-scope-verify.mjs deployVerdict (MISSING, exit 2 UNDEPLOYED), tra3057-verify.mjs leg 1 (a bare `catch { contains = false }` — it had NO cannot-tell state at all), and tra2342-interlock-live-check.mjs's THREE LIVE arms ('Deploy it before arming', against a build already carrying the interlock). These fail CLOSED, so nothing shipped that should not have — the cost is a re-derivation plus a gate people learn to route around, which is how TRA-3678 spent six sessions as a red pin nobody believed. TWO things are graded per subject: the ROUTING (`--ancestry-probe` drives the subject's OWN decision function, not the shared grader) and the EXIT CODE (the subject runs END-TO-END inside the graft against an out-of-process fixture host and must exit non-green AND print `blind-shallow`). That marker is load-bearing: three of the four exit 3 on an unreachable host too, so exit code alone passes on a network blind — the first cut of this harness scored 23/23 that way with the fixture server deadlocked behind spawnSync, and ARM 0d now pins it. Controlled in BOTH directions: every grafted arm has a complete-clone twin that must still ANSWER, and a genuine non-ancestor there must still read ABSENT — 'blind on everything' is the obvious way to fail this fix. Pre-fix rules are quoted from the shipped source at 0270b255 and replayed against the MEASURED graft rc, so a fix arm cannot go green because the repro stopped biting. NOT in pretest, same reason as the other two: it needs a COMPLETE clone.
~~~~

## `scripts.//check:dark-gates`

~~~~text
TRA-4487 (off TRA-4486 ask 3) — the gap BETWEEN check:strands and check:spent-oneshot, proven by TRA-3058: a clock-gate leaf rests `todo` on purpose (todo is the SANCTIONED rest for a parked-ready leaf, so it is out of the strand predicate BY DESIGN), its only wake path is a one-shot routine, every fire dies, the routines are archived — and now NEITHER detector can see it: check:strands keys on `blocked`+empty and skips todo; check:spent-oneshot scans ACTIVE routines and archived ones are out of scope. TRA-3058 sat 34 days holding TRA-3052→TRA-3057→TRA-3110 (a live security ticket) dark with ZERO recovery actions raised. Predicate (QuantTrader wording, verified against the incident): OPEN issue AND (1) blocks ≥1 OPEN issue AND (2) own blockedBy empty or no open entry AND (3) updatedAt older than --stale-days (default 7) AND (4) no FUTURE monitorNextCheckAt — a SPENT monitor (null after fire) and a PAST one both fail (4); they are not wake paths. ⛔ THE LIST PROJECTION SERVES NEITHER `blocks` NOR `blockedBy` (measured 2026-09-10): `(row.blocks ?? []).filter(...)` is [] on every list row and brands the whole company 'blocks nothing' = CLEAN. Edges are graded ONLY from a per-row detail GET, spent only on rows already open+stale+unmonitored (216 of 4391 on the founding run). Fails CLOSED everywhere: zero rows scanned, an unrecognised status (row OR blocks[] entry), a dropped predicate key, a failed detail GET, and the page-cap are all BLIND, never a grade. First live run 2026-09-10: FLAGGED 20 across 3 owners, fixture TRA-3553 found exactly as TRA-4486 predicted (TRA-3058 now `done`; TRA-2918 inside the 7d debounce — it re-flags if untouched). The remedy is OWNER-SHAPED: re-derive the gate, then finish / arm a real future monitor / route — ⛔ NEVER close un-derived, that mints a shape-2 silent strand on every dependent. Cadence: routine efd820ff (the same fire that runs check:strands) runs this third. AMENDED BY TRA-4848 (2026-09-24): the founding predicate graded RESTING PATH 1 ONLY and printed "NO wake path at all" — false for ≥3 of the 5 rows on the 09-24 fire, each holding a pending `wake_assignee` interaction with no expiry (TRA-3100's was a 49-day `human_only` board card), i.e. the exact premise that licenses the un-derived close the same report forbids (defect class of TRA-4078/TRA-4084). It now derives ALL FIVE paths: (1) FUTURE monitor, (2) open blockedBy, (3) pending interactions — read from `/api/issues/{id}/interactions`, NEVER the issue GET, which OMITS the `interactions` key entirely so a presence-test reads absent as zero, (4) `reviewAttention.state == "covered"`, (5) non-null `activeRecoveryAction`. Verdict split by remedy: DARK (no path of any kind — needs a wake path; pages) vs STALE-PATH (a live path exists but the row is stale — escalate to whoever can answer; card age + effectiveResolverPolicy printed; ⛔ do NOT re-post the card, a replacement auto-expires the prior one and EXPIRY DOES NOT WAKE). Every row prints `pending interactions: N` (matching check:strands) so the verdict is auditable. TRAP 7: a failed interactions GET is "a card exists" (STALE-PATH, `UNREAD` printed), never DARK — a route error must not manufacture the close-licensing claim. 0 CLEAN · 1 DARK · 2 usage · 3 BLIND · 4 STALE-PATH; BLIND > DARK > STALE-PATH > CLEAN.
~~~~

## `scripts.//drain:strands`

~~~~text
TRA-3541 — the WRITE half of check:strands. The board ruled on 2026-08-13 that hand-draining the blocked+empty recovery-strand cohort every 12h is not a fix; detection was owned and live, the DRAIN was owned by nobody. This does NOT re-implement the cohort predicate: it imports sweep() from check-blocked-empty.mjs and executes the repair that detector already derives, so the population written to is definitionally the population graded. Per row: ONE combined PATCH {"status":<class-dependent>,"comment":<audit trail>} (TRA-4043 — the comment rides in the same write, so a row either drains whole or is untouched; no order exists for a budget cut-off or a run lock to truncate mid-row). History: the assignee write was dropped 2026-08-26 (unexecutable, 409 on every row — TRA-4041; the re-home is the platform's), and the separate comment POST was folded into the PATCH 2026-09-24 (TRA-4043, halving the per-row budget cost). The status is CLASS-DEPENDENT (TRA-4063): `todo` for a DURABLE STRAND, `done` ONLY for a SUPPRESSING LEAF (a routine_execution spawn gating nothing, where `todo` would switch its own routine off); an UNCLASSIFIABLE row gets NEITHER. ⛔ NEVER `blockedByIssueIds` (an empty write-key list reproduces the unroutable born-blocked state); all bans are enforced on the request BODY by gradeWriteBody(), not on the rendered report. ⛔ BUDGET-AWARE (TRA-4043): the platform caps a run at 20 cross-issue writes (enforce since 2026-08-11); at 1 write/row the ceiling is 20 minus `--budget-spent`, rows beyond it are NOT_ATTEMPTED_BUDGET — excluded from the failure count entirely, never folded into WRITE_FAILED (the 2026-08-26 fire reported 19 never-attempted healthy rows as write failures) — and the fire exits 1 DEFERRED: healthy, the budget resets per run and the next fire continues. A live 429 cross_issue_influence_cap_exceeded is AUTHORITATIVE (the local counter cannot see what the surrounding run spent) and defers the rest of the plan. A 403 cross_issue_influence_run_context_required is TRANSIENT early-run state (TRA-4299), retried once then RUN_CONTEXT_DEFERRED (colours REMAINDER) — never FOREIGN, never FAILED. Success is verified by RE-READ, never by the 2xx — a PATCH that returns 200 and does not stick reads as FAILED. ⛔ Repair is ASSIGNEE-SCOPED and rank does not lift it, so no single seat can drain the company: a row this seat cannot write is reported FOREIGN and drains from the OWNER's twin arm. Exit 0 VACUOUS (no drainable row existed — NOT a pass, nothing was exercised) / 0 DRAINED / 1 REMAINDER (ineligible, FOREIGN, run-locked, unclassifiable or run-context-deferred rows somebody must come for) / 1 DEFERRED (budget ran out; remainder owned by the NEXT fire) / 2 FAILED / 3 BLIND. Off-roster+unassigned rows and shape-2 rows are ACKNOWLEDGED: printed in full every fire, but they do not colour the exit, because both classes have a named owner and a daily red gets tuned out. The carve-out is a RULE (off-roster assignee), never an identifier list, so it expires by itself.
~~~~

## `scripts.//check:phantom-rest`

~~~~text
TRA-2422 — a non-terminal leaf with NO live continuation path. ⛔ TRA-3604 — a MONITOR IS NOT A ROUTINE. `monitorNextCheckAt` and its five siblings live on the ISSUE ROW, a different table, and they are the live path for 32 of the 231 non-terminal leaves (27 of 62 `in_review`) on 2026-08-13. Any sweep that resolves leaves against the routines table alone reads every one of them as unmonitored — a false-positive source ~5x the real signal. This check reads them off the LIST route (which carries all six), and ⛔ their ABSENCE IS SILENT, so key PRESENCE across the swept population is asserted and its loss is BLIND, never a page of findings. ⛔⛔ A FIRED MONITOR SELF-CLEARS: `monitorNextCheckAt` goes null, `monitorLastTriggeredAt` is stamped, `updatedAt` moves ~50ms and nothing else changes — the residue is BYTE-IDENTICAL to a leaf that was never monitored, and no wake, recovery action or `missing_disposition` follows. Calling that PHANTOM_REST prescribes the WRONG REPAIR: PHANTOM_REST says 'arm the monitor that was promised', but on TRA-3436 (fired 08-13T13:30:04Z, 1 attempt) the fire was already SPENT and arming a second one would have been wrong — the repair was to run the grade it should have produced. Hence FIRED_MONITOR_NO_DISPOSITION, its own class with its own repair sentence. ⛔ `monitorLastTriggeredAt` ALONE IS NOT THE TEST: a monitor that fired and RE-ARMED carries both fields and is resting (future `nextCheckAt` wins), and a fired BACKSTOP behind a still-open primary is legitimate rest — 4 of the 5 measured class-B leaves were healthy (TRA-2631/TRA-3104 behind pending cards, TRA-2305/TRA-2937 behind live linked routines). The discriminator is not 'did it fire', it is 'is a primary path still open', so this class is reached only after all six paths say no. ⛔ A 6h GRACE WINDOW (`--fire-grace-hours`) IS LOAD-BEARING: the first live run flagged TRA-3496 **132 seconds** after its fire, i.e. faulted an assignee for not answering a wake delivered two minutes earlier. Fires inside the window are FIRED_IN_FLIGHT, a path, not a strand; the real strands measured were 2–11 DAYS old. ⛔ The A/D/B/C census is PRINTED AT EVERY VERDICT INCLUDING CLEAN — before TRA-3604 the report published findings with no denominator, so '5 spent monitors' and '31 leaves never monitored at all' could not be told apart and neither could be rated.
~~~~

## `scripts.//check:routine-dispatch`

~~~~text
TRA-2331 — grades the claim check:phantom-rest MAKES. That check clears a leaf as monitored on `active` + `enabled` + a FUTURE `nextRunAt`, and ⛔ `nextRunAt` IS A PROMISE, NOT A RECORD: it is recomputed from the cron and moves forward whether or not the fire ever dispatched, so a routine whose every dispatch is failing reads byte-for-byte identical to a healthy one. The discriminator is the run history. Reads the LIST route only — ⛔ that route OMITS the `recentRuns` key entirely (absent, not []) so `(r.recentRuns || []).length === 0` brands the whole company never-dispatched; it DOES carry `lastRun`, which is the tail verdict in one call. Exit 0 clean / 1 per-routine findings / 2 FLEET (spans the roster — ONE platform condition, do NOT file a ticket per routine) / 3 blind. Zero graded rows is BLIND, never clean. ⛔ TRA-3487 — a `failed` tail is now ATTRIBUTED against the durable owner-pause tape (`activity_log` agent.paused/agent.resumed/agent.terminated, read from the platform's Postgres) AT THE FAILURE INSTANT, splitting into OWNER_PAUSED / OWNER_TERMINATED / OWNER_STATE_UNKNOWN. All three stay findings but are HELD OUT of the FLEET test, because FLEET is a claim about the DISPATCHER and per TRA-2871 the usual TRUE cause of a roster-wide burst is that the OWNERS WERE PAUSED — the old banner asserted 'ONE platform condition' unconditionally and so reproduced the TRA-2867/2871 misdiagnosis twice a day. The banner now NAMES its subject, and a roster-wide pause prints an OWNER PAUSE BURST instead. ⛔ Attribution FAILS CLOSED: an unreadable tape, a coverage floor that does not reach the failure, or an `agent.updated` PATCH touching `status` (the platform logs the KEY, never the VALUE, and agent_config_revisions does not track `status` at all) each yield OWNER_STATE_UNKNOWN — never 'the owner was fine'. ⛔ Do NOT read the tape from /api/../activity: its `action` filter is SILENTLY IGNORED and limit=1000 returns 500 rows (~3.4h). ⛔ TRA-3372 — every EXECUTION_ISSUE_ABANDONED row now NAMES the closing actor and the ASSIGNEE AT CLOSE, read from the same platform Postgres as the pause tape (`activity_log` `issue.updated`, which UNLIKE `agent.updated` records the VALUE and a `_previous` block, so status and assignee moves are recoverable exactly). The finding routes to the ROUTINE OWNER, which is the right default — but on the founding run 3 of 4 rows were closed while the issue belonged to SOMEBODY ELSE (the strand recovery had re-homed them first), so the owner had to reconstruct the actor by hand from the activity log before they could answer 'was this intentional?'. ⛔ The assignee at close is REWOUND BACKWARD from the current assignee, because `recovery.*` re-homes WITHOUT logging `assigneeAgentId` and a forward replay therefore reports the ORIGINAL owner — the exact error being fixed. ⛔ The close is the LAST transition INTO the status the DISPATCHER named, never the newest write (TRA-3196 was re-blocked and re-homed 33h after the close its run was graded on). ⛔ The ACTOR is `actor_type`/`actor_id`, NOT `agent_id` — that column is NULL on every system and user write, i.e. on exactly the writes an owner most needs named. Every failure path is UNATTRIBUTED and prints why; an unreadable tape must never read as 'the owner closed it'. ⛔ ALSO REMOVED: the report used to assert 'under `skip_missed` the slots they lost are gone — nothing will replay them'. That is FALSE. `skip_missed` is the CRON catch-up policy and it is not the only path to a fire: measured 2026-08-13, all three TRA-3372 routines missed BOTH their 07-31 and 08-07 on-slot fires and BOTH were replayed OFF-CRON by a restart backlog drain (08-02T00:08Z, 08-11T13:30Z), one of which ran to completion. This check reads the TAIL, not the schedule ledger, so it cannot tell a lost slot from a replayed one. Do not put it back. ⛔ TRA-3603 — a DESIGNED RETRY RUNG'S SKIP IS NOT A BROKEN TAIL. A date-pinned acceptance ladder is commonly built as PRIMARY+RETRY pairs on ONE routine under `skip_if_active`: the primary lands a carrier, and the retry ten minutes later is refused BECAUSE IT LANDED. This check called every one of those a broken dispatch tail, so its false-positive rate rose with how carefully the routine had been designed — the wrong incentive, and it cost a full hand re-derivation of routine `098ce476` (filed as TRA-3601, ruled false positive). The discriminator is an IDENTITY, not proximity: the skipped run's `linkedIssueId` IS the issue that refused the fire, and a sibling trigger's `lastResult` (`Created execution issue <uuid>`, anchored) names the issue IT created — same uuid ⇒ the thing that blocked this fire is provably this routine's own carrier for the slot. ⛔ SCAN `routine.triggers`, NEVER the armed subset: retiring a spent one-shot arm is the standing remedy for the zombie 2027 `nextRunAt` that check:spent-oneshot files, so the covering PRIMARY is usually `enabled:false` by the time anyone looks (it was on `098ce476`) — reading `armedTriggers()` here fails on the founding fixture and works LESS well the better the owner's hygiene. ⛔ THE LABEL IS NOT THE INSTRUMENT: `t.label` says "RETRY of the 14:15Z slot (no-op if it landed)" and it is prose — a self-colliding routine can carry the word RETRY and a correct one can carry no label — so it is REPORTED and never DECIDES. ⛔ `coalescedIntoRunId` is still never read (Trap 3b, stamped on both branches). ⛔ A WINDOW IS REQUIRED (`--retry-cover-window-min`, default 180) or a STUCK CARRIER buys permanent silence: an issue that never closes matches the identity on every future slot, and a whole cohort collapsing into skips against one wedged carrier is the REAL self-collision this check exists to catch. Fails CLOSED everywhere — unparseable `lastResult`, absent `linkedIssueId`, unreadable `linkedIssue.status`, or an out-of-window fire all leave the row a DISPATCH_SKIPPED finding, because trading this check's false positives for a false negative on the one state it owns is not a fix. Coverage that DIED (carrier `blocked`/`cancelled`) is its own state DISPATCH_SKIPPED_CARRIER_ABANDONED — still a finding, held OUT of FLEET on the EXECUTION_ISSUE_ABANDONED reasoning. ⛔ SUPPRESSED ROWS ARE PRINTED IN FULL at every verdict INCLUDING CLEAN, with the sibling trigger, the carrier, its status and the gap: a suppression nobody can see is one nobody can audit, and this check's whole history is states that read identically in pass and fail.
~~~~

## `scripts.//check:carrier-dispatch`

~~~~text
TRA-3529 — the NEXT link after check:routine-dispatch. That one grades the DISPATCH TAIL (did the fire produce a run?); this one grades what happens AFTER a successful dispatch: was anybody ever woken on the issue that run created? ⛔⛔ RUN `status` MEASURES ISSUE CLOSURE, NOT EXECUTION — a run sits at `issue_created` until its linked issue reaches a terminal status, and bb11cef4's run flipped to `completed` at the exact instant a sweep closed TRA-3513 BY HAND, no work in between. So `issue_created` (which is in the sibling's RUN_SUCCESS set, correctly, for its own subject) and `completed` both say nothing about whether the order was executed. The discriminator is `issue.startedAt` — the platform's record of a CHECKOUT — and ⛔ it is NOT `assigneeAgentId` (873 of 877 carriers carry one; the routine propagates it correctly) and ⛔ NOT `zero comments` (TRA-3529's stated discriminator, which HEALS WITHOUT A DISPATCH the moment a sweep closing the carrier writes a comment on the way past — a lagging proxy that fails toward CLEAN). ⛔⛔ TRAP: `lastRun.linkedIssue` on the LIST route serves SIX keys (id/identifier/priority/status/title/updatedAt) and `startedAt` IS NOT AMONG THEM, so reading it there brands all 197 carriers never-dispatched — this check FANS OUT to GET /api/issues/{id} and exits BLIND on an absent key. ⛔⛔ TRAP: routine status is NOT filtered — 163 of 197 carriers sit on ARCHIVED routines because a one-shot's own prose orders it to archive itself as step 1, so the `active`-only population that is right for the sibling check would hide 83% of carriers INCLUDING the self-archiving deploy trains that are the urgent case. Exit 0 clean / 1 findings / 2 SYSTEMIC with the subject NAMED / 3 blind. ⛔ A pile of findings on ONE assignee is that agent's QUEUE, not a dispatcher fault: measured 2026-08-13, the same agent's MANUALLY-created issues starved at 67% vs 62% for routine-spawned ones while every other assignee sat at ~0% regardless of origin, so the variable is queue depth and the founding cohort only looked routine-specific because it was selected routine-only. Only a spread across the roster leaves the dispatcher as the remaining subject. CARRIER_CANCELLED_UNWORKED (a deliberate retirement) stays a finding but is held OUT of the systemic test so retired routines cannot put a permanent floor under the verdict. ⛔ Grades ONE carrier per routine (the newest), so a worked newest carrier HIDES an abandoned older one — a NAMED blind spot, printed every run, not a silent cap.
~~~~

## `scripts.//check:carrier-dispatch:cohort`

~~~~text
TRA-3535 (board ruling on TRA-3532 item 6) — the SECOND ARM of check:carrier-dispatch, and the reason the check's own headline is no longer an assertion. The routine arm reports QUEUE STARVATION, which is a claim about an AGENT'S WHOLE QUEUE — but its population is carriers-by-routine, so the evidence for that claim was structurally out of reach: on 2026-08-13, 17 of LeadDev's 22 starved rows were MANUALLY created (77% of the cohort the banner is about) and every one of them was hand-counted, first in TRA-3529 and then again in TRA-3532. Twice hand-counted is the signal. This arm changes the POPULATION, not the predicate: every issue created in the window, partitioned by (assigneeAgentId, originKind). ⛔ THE DISCRIMINATOR IS UNCHANGED and deliberately so — `startedAt`, with executionLockedAt / checkoutRunId / executionRunId as corroboration (the predicate is that NONE of the four is set; reading startedAt alone brands a row never-dispatched while the platform holds an execution lock on it). Run `status` and zero-comments both fail toward CLEAN and are not used here either. ⭐ NO FAN-OUT IS NEEDED: verified 2026-09-22 against the single-issue GET, the FULL issues-list row serves startedAt / originKind / executionLockedAt / checkoutRunId / executionRunId with IDENTICAL values — unlike `lastRun.linkedIssue`, which drops startedAt on every row. ⛔⛔ BUT NEVER `?view=compact`: a compact row NULLS fields the full GET has, which would brand the entire board starved. ⛔⛔ THE LIST ROUTE HAS NO createdAfter FILTER AND IS NOT ORDERED BY createdAt (measured: offset 0 starts TRA-3926, offset 200 starts TRA-1662), so there is NO early exit — the whole board is enumerated and filtered in memory, and hitting the page cap is BLIND, never a partial pass. ⛔⛔ THIS ARM CANNOT BE RUN BACKWARDS, which is the single most important thing about it: the starved population HEALS. The exact cohort that read 68% live at 2026-08-13T10:00Z re-reads 10% today, because 15 of LeadDev's starved rows (13 manual) were dispatched AFTER the read and `startedAt` records only THAT a dispatch happened, never that it was late. So a retrospective query systematically under-reports and the 08-13 hand-count is NOT reproducible by pointing this check at 08-13; `--cohort-since` is for inspection and prints a loud warning. Grade the window that is open NOW. ⛔ WINDOW IS 72h, NOT 24h: measured 2026-09-22T19:26Z a 24h window graded ZERO rows (all 12 issues that day were <4 min old and inside grace), and a default that reads BLIND on an ordinary quiet afternoon is a default that gets muted; 68% starvation is not hidden by three days. ⛔ CORRECTION 1 — NEVER-CHECKED-OUT IS NOT NEVER-HANDLED: 6 of the 25 never-dispatched rows on 08-13 were already CLOSED (TRA-3493/3511/3513 graded by replay, TRA-3521/3520 done, TRA-3528 a cancelled probe), so ISSUE_UNDISPATCHED_SWEPT and _CANCELLED are REPORTED but PARTITIONED OUT of the rate — folding them in over-reports the headline by ~24% (21 vs the true 17). ⛔⛔ CORRECTION 2 — CALIBRATED ON THE MEASURED SPREAD, NOT ON ZERO: TRA-3532 carried 'every other assignee sits at ~0%' and that is FALSE — CTO's manual rate in the same cohort was 18% (3/17). A threshold calibrated against ~0 convicts the next agent who has a slow day, gets argued with once, and is muted. The real 08-13 separation is LeadDev 67% (22/33) against a POOLED rest of 10.5% (4/38) = 6.4x, so BOTH a 35% absolute floor AND a 2.5x multiple over the pooled rest are required, and each alone is wrong (floor alone convicts a board having a bad week; multiple alone convicts 12% against a rest at 4%). The rest is pooled by ROWS, not as a mean of per-agent rates, or an agent with 2 rows swings the baseline as hard as one with 200. ⛔ A RATE OVER A HANDFUL OF ROWS IS NOT A RATE — n>=8 to be eligible as the subject (live CFO reads 25% off 1 row of 4). ⛔ UNASSIGNED IS NOT AN AGENT: undispatched BY CONSTRUCTION, sits at 100%, would win every concentration test forever — reported as its own class, excluded from the maths. ⛔ <2 eligible assignees means the SPREAD is UNMEASURED and concentration is SUPPRESSED; a 'concentration' over one queue is not a concentration. ⛔ A row inside GRACE is excluded from the DENOMINATOR (a dispatch in flight is not a healthy row — counting it dilutes toward CLEAN), but a DISPATCHED row inside grace IS graded: grace excuses a dispatch still in flight, it does not un-read one that landed. ⛔ QUEUE stays the subject when findings concentrate ACROSS ORIGINS — that is what rules out an origin-specific fault — and routes to the owner and the board, NEVER to the platform, and NEVER as a ticket per starved issue. DISPATCHER is tested only if QUEUE did not fire. ⛔ THE WORST ARM WINS the exit code: taking the better of the two would let a CLEAN routine slice launder a starved manual slice, which is the blind spot this ticket exists to close. ⭐ BOTH-DIRECTIONS CONTROLS, every invocation: a known-bad cohort reconstructed from the measured 08-13 spread that MUST fire, and a known-good BUSY board at the live spread that must stay SILENT, built from the SAME generator so the only difference is the rate — plus a selftest assert that the arm reached SYSTEMIC *and* a silent verdict *and* BLIND, because a detector only ever shown firing cannot tell a starved queue from a busy one. Live 2026-09-22: FINDINGS, not systemic (top eligible CTO 8.3%, LeadDev manual 0/12) — the 08-13 condition has resolved.
~~~~

## `scripts.//check:slot-loss (1 of 2)`

~~~~text
TRA-3713 — the THIRD link, and the one none of the others can reach: not whether a fire produced a run (check:routine-dispatch), nor whether anybody was woken (check:carrier-dispatch), nor whether the commit is live now (check:deploy-train-window), but WHEN the fire happened relative to the slot it was for. Carrier `e7ccfe59` was due 2026-08-13T21:50Z, did not fire, and was replayed by the 00:21Z restart drain 2h31m late — into a window whose embargo reasoning had already expired. Every instrument above read that as healthy. ⛔⛔ THE INTENDED SLOT IS NOT READABLE AFTER THE FACT: `nextRunAt` has already rolled forward (to 2027 on a one-shot), and nothing on the routine, trigger or run records the slot the fire was FOR. It is RE-DERIVED by evaluating the cron BACKWARDS from `triggeredAt` in the trigger's own timezone (scripts/lib/cron-slot.mjs, unit-tested by check:cron-slot). ⛔ THE TIMEZONE IS PART OF THE ANSWER — the fleet mixes ET and UTC triggers, and a UTC-only evaluator is wrong by 4h, which is the same magnitude as the lateness being measured, so the error reads as a different lateness rather than as a bug. ⛔⛔ "THE NEAREST SLOT" IS THE WRONG RULE AND IT ERASES THE DEFECT: on a WEEKLY cron a replay more than half a week late is nearer to the NEXT slot, so nearest-slot laundered three real 3d17h losses (`81928e50`/`a986323e`/`f28ea628`, all replayed by the 08-11T13:30Z drain) into harmless "early" fires. The scheduler already records the answer — `trigger.nextRunAt` still pointing AT a slot proves that slot was never consumed, so the fire belongs to a PAST one. A future slot is admissible ONLY once nextRunAt has moved BEYOND it. ⛔⛔ THE CREATION FLOOR IS LOAD-BEARING: a cron evaluated backwards past the routine's own `createdAt` invents a slot every time, and `618de42d` read "72h late" for a Monday two days before it existed — an accusation manufactured out of calendar arithmetic that looks exactly like a real missed weekly slot. ⛔ A fire attributable to NO slot (`d038b618`: dispatched while its only slot was still armed, created after the previous one) is SLOT_UNATTRIBUTABLE, its own state — reporting it as "24h early" or "8735h late" would be inventing a number in order to have one. ⭐ THE FAIL THRESHOLD IS NOT A CONSTANT: where a carrier declares its window as data (the ```deploy-order `deadline` TRA-3533 already mandates) the check grades against THAT — WINDOW_EXPIRED (exit 5, the incident: it ran on reasoning that had already expired) vs SLOT_LOST_WINDOW_HELD. The numeric band (on-time <= 300s, fail >= 60m) is the FALLBACK for undeclared windows and is two-sided on purpose, with DRIFT reported and never a finding, because a band that fires on ordinary scheduler jitter is muted within a week. ⛔⛔ A RESTART DRAIN IS ONE EVENT, NOT N FINDINGS — fires clustering inside 120s are reported as one RESTART_DRAIN with its members listed, or the report prescribes 13 repairs for one process gap. ⛔ ARCHIVED ROUTINES ARE IN THE POPULATION (the self-archiving deploy trains are the urgent case; `e7ccfe59` is archived). ⛔ NAMED BLIND SPOT, printed every run: the list route carries ONE run per routine, so an on-time newest fire HIDES an older lost slot. ⛔ Exclusions are printed with counts by cause at EVERY verdict including CLEAN, and zero graded rows is BLIND, never clean. 0 clean / 1 SLOT_LOST / 2 usage / 3 BLIND / 4 UNGRADED (a deploy train whose slot could not be established — the urgent class unmeasurable is NOT the urgent class fine) / 5 WINDOW_EXPIRED.
~~~~

## `scripts.//check:cron-slot`

~~~~text
TRA-3713 — unit controls for the backward cron evaluator that check:slot-loss stands on. Pins the two pre-registered rows, the ET-vs-UTC divergence, both DST edges (a 02:00 slot on 2026-03-08 does not exist and is never invented; an ambiguous 01:00 on 2026-11-01 resolves to the FIRST occurrence), the refusals (named months, macros, dom+dow both restricted), and the four attribution rules. In pretest: it is pure and ~250ms.
~~~~

## `scripts.//check:deploy-build`

~~~~text
TRA-3695 — the DEPLOY'S OWN build command, in front of `main`, blocking. `070a188a` reached origin/main with a hard TS error and EIGHT commits landed on top of it, every one un-deployable, until a live deploy to the money host was the thing that noticed — bqb1 was then bisected forward commit-by-commit and briefly served two off-tip mid-train builds. ⭐ THE DEFECT IS AN ASYMMETRY, NOT A MISSING CHECKER: the ADVISORY check (lint) already ran pre-deploy and is explicitly non-blocking, while the BLOCKING one (`pnpm build`) ran only at deploy time. CI compiles the packages but runs ON push to main, which is AFTER the commit is on the branch the deploy pulls from — a detector behind the branch, which is what we already had. ⛔ `tsc -b --force` TYPE-CHECKS THE `.test.ts` FILES, so a test file breaks the BUILD without ever running: "tests pass" is NOT evidence, and neither is `pnpm typecheck`, whose project graph is not the graph `packages/server` compiles. This gate therefore runs NO PROXY — it PARSES the deploy's build script out of package.json and runs those segments verbatim, and reads BLIND (3) rather than guessing if the shape changes. ⛔ WHICH command the deploy runs is not knowable from this repo: render.yaml pins `web:build` while the live bqb1 log prints `[render-build]`, and they are NOT the same chain (`render-build` hardcodes four packages, `web:build` globs five — `packages/agents` is in the glob and not the list). So the gate grades the UNION of both and does not have to be right about which the dashboard holds; a render.yaml re-pointed at a third script reads BLIND. ⛔ THE SUBJECT IS THE COMMIT, NEVER THE WORKING TREE — built in a throwaway `git worktree` with node_modules junctioned in. Grading the tree in place is wrong in BOTH directions: an unstaged fix passes a broken commit (the exact failure this closes), and an unrelated dirty file fails a clean one — and a gate that is red for reasons the author did not cause gets `--no-verify`'d on day one, which is the same end state as no gate. ~70s. ⛔ BLIND REFUSES THE PUSH TOO: "could not check" and "checked and it is fine" must never share an exit code. 0 clean / 1 BROKEN (the incident) / 2 usage / 3 BLIND; precedence BLIND > BROKEN > CLEAN. ⭐ `core.hooksPath` is LOCAL config and is NOT committed, so the hook file is inert until something arms it — that is `prepare` → `hooks:install`, and whether it is actually armed is MEASURED by `--verify-hook`, never assumed. Controls run on LIVE HISTORY, not a synthetic plant: `070a188a` (the real break) must read BROKEN and reproduce the Render log's own `(202,66) error TS18047`, and `1ff4fa7b` (the fix) must read CLEAN — without that second arm a gate hardwired to exit 1 would pass.
~~~~

## `scripts.//check:deploy-train-window`

~~~~text
TRA-3533 — the residual of TRA-3529, and the OUTCOME leg its sibling structurally cannot reach. check:carrier-dispatch grades whether anybody was WOKEN on a routine's carrier; that is the right question about the queue and the wrong one about the deploy, because a carrier can be picked up promptly and the deploy still be refused, or abandoned entirely and the commit still be live off another path. This grades the OUTCOME: is the ordered commit an ancestor of the bytes bqb1 is executing? ⭐ THE POINT IS THAT IT DOES NOT REQUIRE THE CARRIER TO HAVE BEEN WORKED — a lateness check written INTO the carrier's own prose ("fail loudly if you read this past T") is structurally blind, because it only runs if somebody runs the carrier, which is the exact event whose absence IS the defect. ⛔ A deploy train must carry a machine-readable ```deploy-order block (commit/host/deadline); nothing on the board recorded WHAT was ordered or BY WHEN as data, which is why the 2026-08-13 disposition of TRA-3493/TRA-3511/TRA-3513 had to be a human re-derivation from prose. `deadline` MUST end in Z — crons are evaluated in ET and the windows are written in UTC, and a bare local time is REJECTED, never guessed. ⛔ SATISFIED means the commit is live NOW, not that it was live BY THE DEADLINE: the timing arm needs Render deploy history and reports UNREAD, never OK — on 08-13 both deploys were satisfied hours late by an unrelated path, which is exactly the case a bare pass would hide. ⛔ TWO EXIT CODES FOR TWO STATES, on purpose: 1 STRANDED (deadline passed, commit not live — the incident, the only code that should page) vs 4 UNGRADED (suspected trains carrying no block — a migration backlog). Folding them into one number buries the incident: the first live run flagged 18 suspected and 0 stranded. ⛔ The prose classifier is a HINT with known false positives (TRA-3413 says "if you find yourself about to redeploy bqb1, STOP"; TRA-3398 quotes an embargo) so it reports SUSPECTED, never "defective" — a grader over other people's artefacts fails toward convicting them. Opt out with `<!-- deploy-order: none -->`. ⛔ Default population is carriers fired in the last 7 days keyed on lastRun.triggeredAt, and the excluded count is PRINTED — the unfiltered first run flagged 53 settled July one-shots for lacking a field that did not exist when they were written. An unreadable triggeredAt is INCLUDED, never dropped. ⛔ Same NAMED blind spot as its sibling: ONE carrier per routine, the newest. Exit 0 clean / 1 STRANDED / 2 usage / 3 BLIND / 4 UNGRADED; precedence BLIND > STRANDED > UNGRADED > CLEAN. Live path proven both directions on TRA-3536 (a real fixture issue whose ordered commit is the SHA ALREADY LIVE, so obeying it is a strict no-op). ⭐ THE TIMING ARM IS NOW REAL AND IS THE POINT: `SATISFIED` is live NOW, and `on_time`/`late` is live BY THE DEADLINE. With RENDER_API_KEY + RENDER_SERVICE_ID set it reads Render's deploy history, binds it to the box by LIVE-SHA IDENTITY (the block names the onrender host `tradingai-bqb1` while the Render service's own name is `TradingAI-`, so a name lookup returns []), and grades whether a SERVED deploy carrying the ordered commit finished before the deadline. A missed window exits 5 LATE — not 1, because nothing is stranded, and not 0, because the window a one-shot exists to hit was missed. It fails to UNREAD and NEVER to LATE on: history that starts after the deadline, a pre-deadline deploy whose commit this checkout does not know, or nothing in the window carrying the commit — a grader over other people's deploys fails toward convicting them. build_failed/canceled deploys never count as "it was live by then". Precedence BLIND > STRANDED > LATE > UNGRADED > CLEAN.
~~~~

## `scripts.//check:spent-oneshot`

~~~~text
TRA-3008 — the complement of check:routine-dispatch. That one grades the DISPATCH TAIL (did the last fire run?); this one grades the FORWARD PROMISE. ⛔ A DATE-PINNED ONE-SHOT CRON HAS NO YEAR FIELD, so after its single fire the scheduler re-arms it ~365d out and the routine keeps reading as live scheduled coverage on every census — a SPENT one-shot is indistinguishable from an ARMED one, and its last run can be perfectly `completed`, so a tail verdict cannot reach it. Finds an `active` routine with an `enabled` schedule trigger whose `nextRunAt` is beyond the horizon (default 120d — wider than any legitimate quarterly cadence). ⛔ ARCHIVING DOES NOT DISABLE THE TRIGGER (measured on c0ca474e: status archived, enabled still true, nextRunAt still 2027) so `status==='active'` is the discriminator, not `enabled` — and archiving IS the remedy. ⛔ The routines route returns a BARE ARRAY: the first run of this census read `body.routines`, matched nothing, and reported 0 against a company that had 8. Exit 0 clean / 1 findings with coverage intact / 2 COVERAGE_LOST (a routine whose only arms are spent) / 3 blind. Zero graded rows is BLIND, never clean. ⛔ TRA-3017 — this check is armed DAILY (routine efd820ff) under prose that says `file ONE issue on exit != 0`, and two coverage-lost rows CANNOT be repaired by the agent running it (routine writes 403 across the assignee boundary; e1b97e28's assignee has DEPARTED, so NO agent can archive it — it needs a BOARD re-home). Left alone the daily fire mints a duplicate issue for the same rows every morning until someone retires the checker. `scripts/spent-oneshot-acknowledged.json` (HAND-EDITED ONLY) suppresses the EXIT CODE and nothing else — acknowledged rows are still printed IN FULL every run, every entry must name a live `trackedBy` issue plus a `reason` or it is IGNORED, and each is pinned to the cron+nextRunAt of EVERY spent arm on the row so a re-arm expires the pin by itself. New verdict ACKNOWLEDGED_ONLY = exit 0 “file nothing”, which is NOT “coverage is intact”. An unreadable ledger degrades to ZERO acks (more filing), never blanket suppression.
~~~~

## `scripts.//check:spent-monitor`

~~~~text
TRA-4081 (off TRA-4078) — the ISSUE-side twin of check:spent-oneshot. That one grades a spent one-shot routine CRON; this one grades a spent one-shot ISSUE MONITOR. `executionPolicy.monitor` is one-shot BY CONSTRUCTION: on fire (scheduler OR the owner's own /monitor/check-now) buildIssueMonitorTriggeredPatch nulls `monitorNextCheckAt`, increments `monitorAttemptCount`, and stripMonitorFromExecutionPolicy DELETES the monitor block. No interval, no re-arm. ⛔ THE PLATFORM'S OWN GUARD CANNOT SEE IT: `invalid_issue_disposition` runs only on the TRANSITION INTO `in_review`, never on the fire path — so the platform moves a row into exactly the state its own guard forbids an agent to create, and raises NO recovery action (activeRecoveryAction was null on 38 of 38 rows, measured 2026-08-26). The row reads `in_review` to every status query and to the board, and is owned by nobody. ⛔ IT FREEZES THE SUBTREE BEHIND IT, not just its own row: TRA-3827 sat spent from 08-20 while TRA-3824 was auto-paused `blocked` with "it will continue automatically when that work is done" — it would not, and both were dark six days. Predicate = non-terminal status AND monitorScheduledBy set AND monitorNextCheckAt IS NULL AND monitorAttemptCount > 0, all four served by the LIST projection. ⛔⛔ TRAP 1, THE ONE THAT ALREADY BIT US: A PAGE LOOP THAT STOPS AT A BELIEVED TOTAL UNDER-REPORTS AND READS GREEN DOING IT — the founding census was taken as "all 1800 issues paged, not sampled" and reported 25; the company holds 4019 and the true count was 38, so THIRTEEN stranded rows were invisible behind an early stop. This pages to EXHAUSTION (a SHORT PAGE ends it) and exits BLIND if it hits its own 200-page safety bound, because not finishing is trap 1 wearing a hat. ⛔ A `blocked` ROW WITH A REAL OPEN BLOCKER IS NOT STRANDED — it wakes via issue_blockers_resolved regardless of its burnt monitor, and paging its owner is noise; `blockedBy` is NOT in the list projection, so blocked rows get ONE extra GET each and an unreadable one is BLIND, never waved through. ⛔ Zero rows scanned is BLIND, not CLEAN. ⛔ A row missing a predicate field is BLIND, not CLEAN — if the projection ever drops `monitorAttemptCount` then `undefined > 0` is false and EVERY stranded row in the company silently passes. ⛔ An unrecognised status is BLIND: guessing terminal hides rows, guessing non-terminal invents them. 0 CLEAN · 1 FINDINGS · 2 usage · 3 BLIND; BLIND > FINDINGS > CLEAN.
~~~~

## `scripts.//resume:routine`

~~~~text
TRA-4064 — the WRITE half of the armed-liveness detector, and the guard the half-finished restore of `ec0f4a75` proved was missing. A burst-free resume is TWO writes — PATCH the trigger (re-times `nextRunAt` forward off NOW so nothing bursts), THEN PATCH the routine root to `active`. The ORDER IS CORRECT; the defect is that the pair IS NOT ATOMIC. On 2026-08-26 step 1 landed at 10:54:50Z and step 2 never ran, and the row sat DARK for ten days reading `enabled:true` with `nextRunAt` a plausible few hours out — because ⛔⛔ STEP 1 DESTROYS THE ONLY HEURISTIC THAT COULD CATCH THE MISS: every other instance of this defect carried a stale PAST `nextRunAt`, so a census could flag it as a clock that is behind, whereas a half-finished restore reads GREEN ON EVERY FIELD EXCEPT `status`. DOING HALF THE REPAIR IS WORSE THAN DOING NONE OF IT — it launders the evidence. The dispatcher selects `routines.status = active` and NOTHING else, so `status` is the sole liveness test and is never inferred from `enabled`/`nextRunAt` (⛔ `nextRunAt` IS A PROMISE, NOT A RECORD — TRA-2331/TRA-4049). ⛔ SUCCESS IS AN INDEPENDENT READ-BACK, NEVER THE 2xx: a status PATCH is measured to return 200 without sticking (see `drain:strands`), so the guard re-GETs and asserts status active AND target trigger enabled AND its nextRunAt FUTURE AND the routine still ours — a mid-run re-home makes the other three claims about a routine belonging to someone else. Step 1 is SKIPPED, and reported as SKIPPED_FUTURE rather than silently, when the clock is already forward: the common invocation is exactly the `ec0f4a75` shape where step 1 already landed and re-patching only recomputes the same boundary (`--force-trigger` overrides). BURST is a SEPARATE AXIS from correctness: a past `nextRunAt` under any catch-up policy but `skip_missed` will flush a backlog on restore, and exits on its own code because "it resumed and will stampede" must never share an exit with "it resumed cleanly". ⛔ Routine management is OWNER-ONLY for every in-company role INCLUDING `role: ceo` — rank does not lift it and there is no fleet-wide resume — so a 403 is reported FORBIDDEN with the measured error text, never BLIND, because an off-roster (departed) owner needs a platform operator and that escalation must carry an observation rather than an inference. ⛔ ARCHIVED IS TERMINAL and is REFUSED, not resumed. Everything fails CLOSED — an unreadable routine, a shape change that hides the triggers array, an absent target trigger, or a read-back that itself fails (writes issued, state unknown, possibly half-restored) all read BLIND. 0 RESUMED · 1 NOT_VERIFIED · 2 usage · 3 BLIND · 4 FORBIDDEN · 5 BURST_RISK; BLIND > FORBIDDEN > BURST_RISK > NOT_VERIFIED > RESUMED.
~~~~

## `scripts.//check:slot-loss (2 of 2)`

~~~~text
TRA-4049 — the third leg. check:routine-dispatch grades the DISPATCH TAIL (did the LAST fire run?), check:spent-oneshot grades the FORWARD PROMISE (is nextRunAt a slot already gone?); this one grades FIRES SINCE LAST EXPECTED SLOT, derived from the cron, per trigger. ⛔ `nextRunAt` IS A PROMISE, NOT A RECORD: it is recomputed forward whether or not the slot was ever served, so a routine that has missed 60 consecutive fires is byte-for-byte identical, on that field, to one that fired an hour ago — which is why on 2026-08-26 28 of 43 active routines had been silent since 08-16 while ALL of them read armed, and both sides of the TRA-4038/TRA-4041 disagreement were reasoning from the only signal available. check:routine-dispatch's own header refuses this axis by name ("Missed-slot counting is a separate, harder instrument; do not bolt an approximation onto this one") — this is that instrument. ⛔ A DISABLED TRIGGER IS NOT A LOST SLOT: 7d30dcfc is active with enabled:false on purpose and was a pure false positive in the cohort of 28; population is enabled:true schedule triggers ONLY. ⛔ A `skipped`/`coalesced` RUN SERVES ITS SLOT — the scheduler did its job and the concurrency policy declined the work; counting those as losses brands every healthy coalesce_if_active routine, so they are served and reported on their own SUPPRESSED line. ⛔ A CATCH-UP FLUSH SERVES LATE, AND LATE IS NOT LOST: on 2026-08-16T13:41Z 23 routines fired within ~2min (a boot flush — runtime-info.json startedAt lines up exactly), so runs are assigned to slots GREEDILY within --max-late and a flush credits only the slots it replays. ⛔ A TRUNCATED /runs PAGE IS BLIND, NOT CLEAN — an unobserved slot rendered as "no run found" is a manufactured accusation. Vixie dom/dow (BOTH restricted => EITHER matches) and DST are handled by enumerating REAL UTC INSTANTS formatted into the trigger's zone, so a spring-forward slot never exists and a fall-back repeat is deduped to one. Zero graded rows is BLIND, never clean. 0 CLEAN · 1 SLOT_LOSS · 2 usage · 3 BLIND; BLIND > SLOT_LOSS > CLEAN.
~~~~

## `scripts.//check:pre-turn-deaths`

~~~~text
TRA-4688 — a run that dies in the adapter BEFORE TURN 1 (session limit, DNS outage, 529, OAuth expiry) leaves the agent reading status:running/idle with errorReason:null, so every roster sweep (incl. the TRA-3300 status:error detector) is blind to it. The signal lives on the RUN: status failed + no token usage + the raw `error` text. ⛔ errorCode is useless for the class — the vendor maps EVERY failed turn to acpx_turn_failed (adapter-utils execute.js ~L3160), so the class is read from the text; an unrecognised text counts as `unclassified`, never drops out. ⛔ The session limit is PER ACCOUNT: on 2026-09-17 it hit 6 seats in the same minutes (100 slots fleet-wide, CFO saw 11 of them) — read the fleet line, not one seat. DARK = the seat's newest TERMINAL run is a pre-turn death (queued retries do not clear it). Fails CLOSED: zero seats, a failed seat fetch, an ignored agentId filter, or a full page still inside the window are BLIND. 0 CLEAN · 1 DARK · 2 usage · 3 BLIND · 4 LOST (deaths in window, all seats since recovered); BLIND > DARK > LOST > CLEAN.
~~~~

## `scripts.//check:data-dir`

~~~~text
TRA-2603 — frozen-baseline ratchet on the unguarded `process.env.DATA_DIR ?? join(__dirname,'..','data')` literal. `??` does not guard a present-but-BLANK value; resolveDataDir() adds the .trim(). Baseline is now ZERO known copies — TRA-2604 migrated all 38 — so any non-zero count is a REGRESSION, not backlog. HAND-EDITED ONLY; never regenerate it from the tree. Exit 1 drift / 2 instrument broken / 3 blind.
~~~~

## `scripts.//check:continuity`

~~~~text
TRA-2634 — grade the level-continuity detector against the REAL stored report archive (both directions, every run) and cross-tab it against the deployed r>=2 session-move rule. Control rows are SCOPED PER BOX and only this HOST's are graded, so run BOTH: `pnpm check:continuity` (bqb1) and `HOST=http://localhost:4242 pnpm check:continuity`. Build @trading-app/shared first — it imports dist. Exit 0 pass / 1 control failed / 3 BLIND, and 3 IS A HOLD.
~~~~

## `scripts.//check:render-service-resolve`

~~~~text
TRA-3743 — both Render helpers defaulted RENDER_SERVICE_NAME to `tradingai-bqb1`, which is the service's SLUG, not its `name` (`TradingAI-`), so `?name=` returned [] and the refusal blamed the API key for a string constant. Offline: the fixture is the live account's two services and a stub that reproduces Render's exact, case-sensitive `?name=` filter. The L-OLD-* cases replay the old expression and assert it still gets the WRONG answer, and the two MESSAGE cases assert the key-problem and name-problem refusals are actually distinguishable — a suite that cannot tell them apart would have passed on the shipped bug.
~~~~

## `scripts.//check:tra2399-controls`

~~~~text
TRA-2399 AC3, END TO END: drives the real CLI over a real socket against a POPULATED book and an EMPTY one, and asserts exit 0 vs exit 4. Needs `pnpm build` first (it computes both payloads with the real gate out of dist/), which is why it is a named step and NOT in pretest — see the header of the script.
~~~~

## `scripts.//tra2420:attribute`

~~~~text
TRA-2420 — rank DATA_DIR's per-trading-day GROWTH by writer off /api/health/storage/detail (ADMIN-gated since TRA-2599, so this needs TRADINGAI_ADMIN_TOKEN or --token=). Exits 2 NOT_DEPLOYED until the deploy carrying the `usage` block lands, which is the only answer live can give before then — hence the paired controls. Exit 5 NO_TOKEN is a MISSING/REJECTED token, deliberately not folded into 3 BLIND: BLIND reads as 'the box is down' and would send you to Render instead of to your shell.
~~~~

## `scripts.//check:pnl-lag`

~~~~text
TRA-2630 AC3 — the T+1 options-credit lag regression guard (TRA-2629). Live-pull only: the defect is a DURABILITY seam, so an in-memory round trip passes while prod is broken. Partitions mode:live (exit 1, escalate) from demo (exit 2) and fails CLOSED to BLIND (exit 3) rather than reporting a green it could not measure. Exit 4 = LIVE CREDIT SHORTFALL (TRA-2635/TRA-2658), a SEPARATE axis that outranks demo lag on the exit code only — the `TRA-2630 AC2` delta verdict always prints ABOVE it and is what AC2 is graded on. AC2 is a DELTA (no NEW lag session on/after the fix-deploy date), never an absolute: the 18 pre-fix rows cannot clear retroactively.
~~~~

## `scripts.//check:fix-live`

~~~~text
TRA-2644 — 'is MY fix in the bytes being executed', which check:deploy-drift CANNOT answer. Ancestry of --commit against the live SHA, NOT drift: drift measures the whole company's merge rate against one box, so it is routinely non-zero on a weekday and a grading gate keyed to it never opens. Requires --commit (no default: a default is how a grader confirms the wrong fix). --window-start=<ISO> adds the arm a point-in-time SHA read cannot have — every build that held `live` across the window must contain the commit, because a rollback mid-session makes that session's EOD row a mixture of two writers. Exit 0 PRESENT / 1 ABSENT / 2 CONTAMINATED (window spans a build without the fix) / 3 BLIND. Fails closed: a SHALLOW checkout cannot distinguish 'not an ancestor' from 'unanswerable', so an unknown SHA exits BLIND, never ABSENT.
~~~~

## `scripts.//check:boot-arm-repairs`

~~~~text
TRA-3810 — the DETECTION half of the TRA-2649 write-path arm. `check:boot-arm` grades the arm's STATE (eligible + drift); this grades whether anyone TRIED to demote it. Bound to `bootArmRepairLedger` (durable, append-only under DATA_DIR), never to `bootArmWriteRepairs` — that counter is a since-boot delta whose `0` reads identically for "clean" and "never exercised" and which a redeploy zeroes, so an alarm on it pins OFF exactly when it matters. Exit 0 CLEAN (zero attempts over a MEASURED window ≥ --min-window-days) / 1 ATTEMPTS (the alarm) / 2 NOT MEASURED (sound instrument, window absent or under the floor — vacuous, never a pass) / 3 BLIND (ephemeral DATA_DIR, swallowed appends, no hydrate, ineligible arm, pre-TRA-3810 build, or unreachable probe). Precedence ATTEMPTS > BLIND > NOT MEASURED > CLEAN: a positive is true whatever the coverage is, so blindness can never demote a real live-money demotion attempt into a plumbing complaint.
~~~~

## `scripts.//check:boot-arm-repairs:acks`

~~~~text
TRA-3852 — `retentionDays` is 180 and the grade was `attempts > 0 → exit 1`, so ONE settled event (the 2026-08-17 TRA-3833 full-settings-save) pinned the alarm ON for half a year and the sixth recurrence of the boot-arm family would have arrived inside that noise. `scripts/boot-arm-repair-acks.json` clears ONE event from the exit code and nothing else: matched on exact `at` + `origin` + `repaired` set + `bodyFields.length` (a new attempt from the same writer has a different timestamp and still exits 1), and only while its `fixCommit` is an ANCESTOR OF THE SERVING COMMIT — roll the fix back and the ack goes VOID and the event alarms again, which the old unconditional exit 1 could not have distinguished. Undeterminable ancestry applies no ack. Acked events are still printed in full every fire, tagged with the issue that settled them, and the resulting pass is the distinct verdict ATTEMPTS-ACKNOWLEDGED (exit 0), never CLEAN. Because that pass asserts an ABSENCE of NEW attempts it falls through the blindness and window-floor gates, so a blind or unmeasured instrument can never reach it. `--no-acks` grades as if nothing were acknowledged — that is the positive control.
~~~~

## `scripts.//check:boot-arm-repairs:shapes`

~~~~text
TRA-3922 — the 2026-08-20T21:21Z pair were bare `{mode}` PUTs from our own Demo toggle (`AccountModeSwitcher.tsx:149`), the control d86230c DELIBERATELY left reachable so an operator can get out of a live arm. Acks match on identity, so no ack generalises to it: the ratified lever appends a fresh durable event on every click and arm B pins RED for the full 180d retention on a designed, benign action — the exact stop-discriminating failure the alarm exists to avoid. The tempting repair, 'a refusal through the ratified UI control is a non-alarming class', is UNSOUND AS AN EXIT RULE and is NOT implemented: `route`, `refererOrigin` and `userAgentFamily` are all headers the CALLER sets (`http-security.ts:40` — 'a client can forge any Origin it likes, so origin-based rejection is not a security boundary'), so keying the exit code on them hands the mute button to exactly the writer this alarm catches. What ships is the half that cannot soften anything: every event is CLASSIFIED (`boot_persisted` / `off_shape_write` / `ratified_ui_shape`) and the class, the per-class census and the off-shape REASONS are printed on every fire, while exit codes stay byte-identical — controls 24-27 pin that a `ratified_ui_shape` event, and an all-ratified-shape ledger, still exit 1. Controls S1-S18 pin that stripping or changing ONE caller-supplied field (missing/unexpected/lookalike/plaintext referer, missing or non-browser UA, other route, multi-field body, `origin: boot`) classifies OUT of the ratified shape. A sound exit-code loosening needs a discriminator the client CANNOT set — a published authenticated-principal identity, which the ledger records on disk (`boot-arm-repair-ledger.ts:139`) but does not publish — and that is a server-side change, tracked on TRA-3922.
~~~~

## `scripts.//check:calendar-coverage`

~~~~text
TRA-4478 — the exchange calendar used to be a hand-typed Set in scheduler.ts covering 2025-2026 only, with a bare weekday fall-through after it. First miss: 2027-01-01, a Friday, ~114 days out at the time of filing. The loud half of that is scheduling; the quiet half is that the SAME calendar keys EOD, risk-day and every per-ET-day fold, so a wrong session boundary silently mis-dates the rows the live-capital promotion gate reads, and the resulting verdict looks completely ordinary. The runtime gate (`calendarEntryGate`) is the last line and the wrong place to first find out — by then trading has stopped on a day the exchange was open. This check runs in pretest and CI so the cliff is seen months early. 0 FRESH · 1 STALE (today is uncovered — the incident) · 2 usage · 3 BLIND · 4 EXPIRING (<=180d runway) · 5 DRIFT (the checked-in bundle is not what the generator's rules produce, or those rules fail their own selftest). Precedence BLIND > STALE > DRIFT > EXPIRING > FRESH. Regenerate with `node scripts/gen-nyse-calendar.mjs` after raising COVERAGE_END_YEAR; never hand-edit the bundle.
~~~~

## `scripts.//check:fleet-bound`

~~~~text
TRA-3737 — the READER for the TRA-3723 fleet-bound detector. `aggregateFleetBound` graded itself on /api/health/live-options-fee-slippage for six days and NOTHING POLLED IT; on 2026-08-20 it was serving `breach` (Σ B_i $558.68 vs a $500 authorization) with the live OTM sleeve ARMED, to an empty room. This is a LIVE NETWORK read of the money host: 0 CLEAN · 1 BREACH · 3 BLIND · 3 UNBOUND. ⭐ UNBOUND is the second half of the reader and the one that is easy to leave out: TRA-3879 made `Σ B_i ≤ A` structural via `φ_eff = min(φ, A/Σ E_i)`, but `Σ E_i` is a READ, and on an unusable or PARTIAL read the sizing silently reverts to the pre-TRA-3879 per-book bound while the route keeps serving `within` for as long as balances happen to fit. `Σ B_i fits` and `the bound that makes it fit is in force` are two claims; grading only the first is how a fix goes idle and the grader still prints clean (measured 28s after the TRA-3879 boot). The test is arithmetic — `φ_eff · Σ E_i ≤ A` over a fleet read that covers every armed book — never a list of `fleetSizingReason` names. It is deliberately NOT in `pretest` — a network call in the offline test path fails BLIND on a plane and gets muted, and a muted reader is the defect this ticket exists to close. The OFFLINE half (`:controls`) is in pretest instead; the LIVE half is driven by Paperclip routine 2b3b32bc (weekdays 08:30 + 16:40 ET), which is outside this repo, outside bqb1, and outside anyone deciding to run it (TRA-3529). ⭐ TRA-3881 — it also names WHICH capital instrument governs the reading. `A/φ` describes the FITTED precondition TRA-3879 retired, so on a healthy post-3879 fleet `fleetCapitalHeadroomUsd` reads −$120.81 beside its own `within`, permanently and by construction — and that field was quoted in this reader's own BREACH escalation checklist. The server now withholds the pair under `phi_fleet_derived` and publishes `fleetSizedMaxSumUsd`/`fleetSizedHeadroomUsd` (`φ_eff · Σ E_i` vs `A`) instead; on pre-3881 bytes the reader labels the stale negative DO-NOT-ESCALATE rather than paging on it. A permanent false alarm gets muted, and a muted alarm and a deleted alarm end in the same place.
~~~~

## `scripts.//check:broker-census`

~~~~text
TRA-3917 — the READER for the TRA-3905 broker submit/fill census (`16ee36d`). TRA-3914 deployed the instrument at 23:03 ET, seven hours after the close, so its first read was a structural NO-RUN: every book `idle`, every structural cell green, and NOTHING about whether the fold is wired. ⛔ An all-zero census is byte-identical to a fold wired to nothing — so this reader refuses to grade rows until GATE 0 holds (`brokerOutcomesEtDay` == the session being read, AND at least one book with `submitted > 0`). 0 CLEAN · 1 FAIL · 3 BLIND · 4 NO-RUN · 5 FORFEIT, precedence BLIND > FORFEIT > NO-RUN > FAIL > CLEAN. FORFEIT outranks NO-RUN because a mid-session boot EXPLAINS an idle fold, and the pm2 memory watchdog's self-restart writes no deploy record — `check:deploy-drift` reads CURRENT straight through one, so `build.startedAt` is the only tell. ⭐ The load-bearing arm is the BREAKER PIN: every other v0nni cell (verdict red, blocked, permissionRejects >= 3) is true of an instrument that merely REPORTS; only `submitted` staying pinned at 3 while `submissionsRefusedByBreaker` climbs says it NOTICED. ⭐ The attribution half filters `voids.recent[]` by `ts >= build.startedAt` — `ts` is epoch MILLISECONDS and `startedAt` is an ISO STRING, and the naive comparison coerces to NaN, matches zero rows, and makes `every(row => row.book !== null)` VACUOUSLY TRUE on a payload where the rows are right there. `--selftest` CONTROL T runs both filters over one fixture to keep that visible; a non-numeric `ts` reads BLIND, never a vacuous pass. The offline controls are in `pretest`; the LIVE half is deliberately not, and is driven by Paperclip routine a52cff69 (weekdays 16:10 ET).
~~~~

## `scripts.//check:journal-stale-opens`

~~~~text
TRA-4254 Ask 4 — a journal row still `OPEN` past its contract's EXPIRATION. A long option cannot be open after it expires: it is exercised or worthless, so the row is a zombie BY CONSTRUCTION, provable with no market read, no broker call and no ledger. Live `092d0877` 2026-09-01: 33 of 60 OPEN rows past implied expiry (21 `single_leg_rv` — TRA-4254's rows — 9 `iron_condor`, 2 `single_leg_otm`, 1 `bear_put_spread`). ⛔ THE SWEEP THAT ALREADY EXISTS READS `clean` OVER ALL 33: `zombie-open-journal-sweep.ts` scopes its universe to `listLiveJournalRows()` because its adjudicator is the LIVE option-fill ledger, which has nothing to say about a demo row — so it published `liveOpenRows: 3 / zombieOpenRows: 0 / clean` on the very response carrying them, a true statement about a universe that excludes 57 of the journal's 60 OPEN rows. Correct scoping, not a bug; this check grades EVERY mode and never reuses that verdict. ⭐ The route projects NO expiration — only `openTs` + `entryDte` (DTE at open) — so expiry is IMPLIED and graded with a 3-day grace: the error direction is deliberate UNDER-reporting. An OPEN row whose `entryDte` is absent/null/non-finite reads BLIND, never clean, and `rowsMode != all` reads BLIND because a paged fold under-counts the population and an under-count looks like repair. 0 CLEAN · 1 STALE · 2 usage · 3 BLIND, precedence BLIND > STALE > CLEAN. ⛔ RED IS THE EXPECTED STEADY STATE until someone rules on the rows — clearing it by deleting or force-closing journal rows restates banked history and is a BOARD call (same posture as TRA-3849's non-session rows). Offline controls only in `pretest`; the LIVE half is deliberately not wired to any gate.
~~~~

## `scripts.//tra2906:rebuild-cash-flow`

~~~~text
TRA-2906 — one-time v1→v2 migration of tradier-cash-flow.<env>.json, which is what makes the broker-fee reclassification reachable at all. The v1 record persists ONE signed number per date and discards the event type, so removing `fee` from a set only affects events NOT YET SEEN: the fees already folded into stored totals stay folded in, permanently mis-signed. ⛔ THE SCRIPT DOES NOT WRITE WHAT THE BROKER RETURNS. Before writing it re-derives the legacy totals from the fetched events under the OLD rule and asserts they reproduce the stored netByDate date-for-date; only then does it apply the new rule, so the classification change is provably the only variable. That control is the whole point — a short or `limit`-truncated fetch would otherwise write a v2 record missing months of deposits, and ⛔ A DELETED DEPOSIT DOES NOT LOOK LIKE AN ERROR, IT LOOKS LIKE A GREAT TRADING DAY. `netByDate` is the only surviving record of those deposits; there is no second copy. Mismatch in EITHER direction refuses. DRY RUN BY DEFAULT — it prints the measured per-date P&L shift so the expected move is MEASURED before shipping rather than asserted from a stale claim. Must run ON THE HOST (needs TRADIER_API_TOKEN + TRADIER_ACCOUNT_ID and the data dir). Exit 0 clean/applied · 1 REFUSED · 2 usage · 3 BLIND · 4 NOOP (already v2).
~~~~

## `pnpm.//`

~~~~text
TRA-1052 — .npmrc sets approve-builds=false, so pnpm skips dependency install/build scripts unless allowlisted here. better-sqlite3 is a native module that relies on prebuild-install (an install script) to fetch its prebuilt binary; WITHOUT this entry the binary is silently absent. TRA-1681 — this comment used to claim the server then 'crashes on require'. IT DOES NOT, and that wrong sentence is why the real risk looked self-announcing for months: sqlite.ts CATCHES the missing module, getStateDb() returns null, every caller falls back to in-memory, and the box boots and serves normally with the agent-spend cap NO LONGER DURABLE across restarts. The only signal is one log line, on a host that exposes no logs to a grader. `GET /api/health/durability` is now the readable verdict, and DURABILITY_POLICY=refuse is what makes it stop a boot. Listed explicitly so `pnpm install --frozen-lockfile` on Render fetches the prebuilt .node (no compilation, so no render-build OOM regression — TRA-1037/1038).
~~~~
